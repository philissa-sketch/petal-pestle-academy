# Petal & Pestle Academy — Build Log

**Current version:** v3.90 · Aug 12–26, 2026
*Corrected at v3.56: this line read **v3.53** while the top row of the version table read **3.55**. Two versions of the truth, three lines apart, in the file whose job is to say which build is running. The tenth hand-typed number in this project to drift.*
*Corrected again at v3.60: it read **v3.59** in this line and **v3.55** two lines below, in the sentence about which build had been run — the same drift, in the same header, four versions later. **The eleventh.** It is why §32 now carries the run status per version rather than as a floating sentence.*
**Automated checks:** **38 scripts in total — 34 `check-*` and 4 `simulate-`/`verify-`.** *Corrected at v3.41: this line used to read "26, plus four simulations", which reads as thirty. The table in §3 headed "The thirty checks" has thirty rows and already includes all four simulations. The word "plus" counted them twice.*
**v3.90 run status:** **all 38 green on Linux**, and `check-lesson-prose` measures **256 lessons, up from 243.** ⚠️ **And check #37 is the first check in this app that can pass without testing anything** — her export and her assessment are both kept out of git on purpose, so on Netlify it finds neither and prints *NOTHING WAS COMPARED* rather than a pass. **A green Netlify run does not mean her document is right; only Gigi's run does.** **The Windows run is Gigi's, and it is the one that counts** — v3.73 is why that sentence is still here.
**Installed at:** `C:\Users\pknot\Downloads\petal-pestle-academy\petal-pestle-academy`
**Runs on:** `localhost:5180` (deliberately not 5173 — that's Lamar's Mission Control)

The nav bar prints the version. If the number on screen isn't the number in the message, the update didn't land.

---

## 1. Version history

| Ver | Date | What |
|---|---|---|
| **3.90** | Aug 26 | ⭐⭐ **SHE HAS SPELLING AND VOCABULARY WORDS, AND EVERY ONE CAME OUT OF THE 256 LESSONS SHE ACTUALLY READS.** Gigi, asked to choose between a published grade-level list and her own material: *"pull from the 256 lessons."* **So the list is not a stack of words beside her week — it IS her week.** The apothecary fortnight gives her *mortar, pestle, sieve, strainer*; Module 1 gives her *seed coat, embryo, germination*. **320 + 320, ten of each for 32 weeks.** ⚠️ **NOT 360** — that came from Lamar's **36-week** year. Hers is 32, and padding would have meant inventing four weeks her schedule does not have. **GENERATED THEN FROZEN**, because a list that recomputed would reshuffle her words mid-year the first time a lesson was reworded — and `check-word-study` asserts every word still traces to a lesson **in its own week**, so the freeze is watched rather than trusted. **⚠️ THE CHECK FOUND A REAL FAULT ON ITS FIRST RUN** — `record` and `step` in both lists, because spelling was filtered against a vocabulary set that was still being built. **⚠️⚠️ AND ITS SECOND RUN EXPOSED SOMETHING WORSE: THE PROPER-NOUN RULE CALLED 623 ORDINARY WORDS NAMES** — among them **`garlic`, `ginger`, `seeds`, `winter`**, exactly the words a herbalism spelling list is for. It tokenised the text *before* looking for sentence starts, and **tokenising strips the punctuation**, so the guard never fired and every sentence-initial word was filed as a name. **⚠️ THE CHECK DID NOT CATCH IT, BECAUSE IT USED THE SAME BROKEN COPY.** The generator excluded a word; the check agreed it was excluded. **Two implementations of one rule agreeing is not evidence** — v3.78 and v3.84 again. Both now import **one** definition from `src/lib/lessonProse.js`. **⚠️ And that lib broke rule 17 on its first save, in the version carrying a header note about rule 17** — a quote inside a regex character class, caught by `check-sources` in four seconds. **⚠️ MEASURING `lesson.hook` MOVED SIX LESSONS OFF THE DEBT LIST AND ONE ONTO IT, AND NOT A WORD WAS REWRITTEN.** `LessonReader` renders the hook at the top of **every** lesson and the check had never read it — it had been measuring **less than she reads**. Six marginal lessons fell under cap; `body-m13-02` rose above. **The prose did not get easier; the measurement got more complete**, and all seven lines are recorded in `readingCaps.js` so a shorter list is never read as progress. **⚠️ THERE IS NO SCREEN YET** — said in the data file, in the check and here, because *correct and unreachable* is this project's most repeated failure. |
| **3.89** | Aug 26 | ⚠️ **A STANDING RULE IN THE HANDBOOK WAS FALSE, AND HAD NEVER BEEN TESTED.** Rule 13 said *"you cannot move a FOLDER — moving is copy-then-remove."* **A move inside one drive is a RENAME** — it does not read the folder, does not need the space, and works on a folder exactly as on a file. **It works across the parent boundary too.** Gigi: *"You can move folders you just cannot delete them. you can create folders too."* **The test took ten seconds and both directions worked first time.** **THE COST WAS SIX HAND-TYPED `rmdir` COMMANDS PER CLEAN-UP, FOR MONTHS**, from someone who does not use the command line. `dist`, `_archive-test`, `.pp-drop` and `pp-update` are in `_to_delete/` now with the loose files and every temporary script this session wrote — **one `rmdir` finishes it instead of six.** *(`Lamar DOC` was already gone; Gigi removed it herself between two checks, and this log does not claim otherwise.)* **⚠️ AND IT IS THE SECOND FALSE STANDING RULE FOUND IN THREE DAYS** — the other was *"the repository being private is the SECOND guard"*, corrected at v3.87. **Both were copied rather than checked, and both were load-bearing.** The rule this produces: **a rule already written down needs OBEYING; a rule that was never TESTED needs testing once.** Two different sentences, and only the first had ever been written down. |
| **3.88** | Aug 26 | ⭐⭐ **THIRTEEN LESSONS HAD NEVER HAD THEIR READING LEVEL MEASURED. NOT ONCE.** The 13 flat Herbalism cards carried no `course`, `quarter`, `week` or `standards`, so no cap could be applied, and they sat on `UNCAPPABLE` for **eighty versions** while every other lesson was checked against her level. **They are what she meets in Quarter 1, Module 2 — the second thing she does all year.** **ALL SIX FIELDS DERIVED FROM `WEEKS`, NOT TYPED**, then read back by a second script that confirmed all thirteen agree with the table. `standards: []` on every one, matching every sibling in modules 1, 2, 4, 6 and 8 — **not one of which claims a Georgia element.** **⚠️ `offGrade` deliberately not set**: it means *"this IS a real Georgia element, just a lower grade's"*, and `herbalismM6.js` already states the rule — writing one without reading the lesson is *"a guess dressed as a citation."* Gigi's call, lesson by lesson. **⚠️ AND A CAP WAS NOT ENOUGH — THE READER COULD NOT SEE THEM EITHER.** `proseOf` had never heard of the older `hook`/`core` shape and returned an **empty string** for all thirteen. Fixing only the tagging would have produced thirteen lessons scoring a perfect zero on every reading measure and **passing in silence** — the exact failure that check exists to catch, **arriving through the door marked fix.** An unreadable lesson is a FAILURE now. **⚠️⚠️ AND THE FIRST DRAFT OF THAT FIX TURNED NINETEEN LESSONS RED FOR SENTENCES THAT ARE ON NO SCREEN.** **243 of 256 lessons carry BOTH `beats` and `core`**, and reading both looked thorough — but `LessonReader` has never rendered both, and **its own header says so.** `core` on those 243 is dead data. **A check that measures what the app does not render is not stricter, it is wrong**, in the direction that costs somebody a day rewriting good lessons. **⚠️ A heading has no full stop**, so joining on a space glued each one to the paragraph below and invented sentences — one measured 29 words. Parts are closed before joining now. **⚠️ THE NEGATIVE TEST FOR THAT CAME BACK GREEN** — with the branch fixed only thirteen short-headed cards reach it — **so the rule is asked directly instead.** *A rule whose failure cannot be observed is a rule nothing is protecting* — `check-khan-units`, v3.76, same sentence. **⭐ §3 OF HER DIAGNOSTIC FILE REWRITTEN CLEAN**, every row computed by calling `khanFor`, `laneFor` and `nextUnitForStrand`. **⚠️ Measurement opens Unit 5, Money and time — the first unit of its lane — not Unit 6**, which is merely the one *called* Measurement. **⭐ `check-diagnostic-record` widened to §3 in the same session**, because its own header said in capitals to do so the moment §3 was corrected — **a rule already written down needs obeying, not rediscovering.** One function called twice, never a copy: v3.78 and v3.84 are what two implementations of one metric cost. **`UNCAPPABLE` is empty and KEPT**, because the check asserts it both ways — and it caught its own fix within a minute. **NINE NEGATIVE TESTS, one of which failed first and was rewritten rather than weakened.** **256 lessons measured, up from 243.** |
| **3.87** | Aug 26 | ⭐ **THE REPOSITORY STAYS PUBLIC — Gigi's decision, made with the counts in front of her.** The open question from the last session is closed. **⚠️ AND THE TWO SENTENCES THAT GAVE THE REASON WERE FALSE.** `.gitignore` said *"which is why the repository is private — the second guard, doing the job it is for"*; `check-publish-safety` said *"the repository being private is the SECOND guard, not this one."* **Both were copied out of Lamar's repo, which IS private. This one is not.** **THERE IS NO SECOND GUARD** — there is one guard, it is `.gitignore`, and `check-publish-safety` is the only thing asserting it. **A reason that is not true is worse than no reason**: it is the sentence that talks the next person out of being careful, and it sat directly above the exclusion list it was undermining. Both corrected in place, dated, **not swapped out silently** — the same convention as the v3.41 and v3.56 corrections. **⚠️ THE COUNT WAS WIDER THAN THE OPEN QUESTION ASSUMED.** It was framed as being about two documents; her first name is in **~60 committed files** — master plan 18, `buildStamp.js` **17**, build log 16, and ~55 others with one or two. **⚠️ `buildStamp.js` RENDERS ON THE LIVE SITE** — `ParentDashboard` line 274 prints the whole changelog in the Grown-Up Corner, **`ParentGate` is a passcode the visitor sets themselves** *("a lock on a door, not a safe" — its own header)*, and `netlify.toml` already records that the free plan cannot password-protect the site. **So the name and the levels were readable by anyone with the URL before the repository question was ever asked**, and pulling the two documents would not have changed it. **The people who actually open it are her mother and her grandmother; the URL is unlisted.** All of that is written into `.gitignore` so the next person decides from the same facts instead of re-opening it. **⚠️ NOTHING IN THE EXCLUSION LIST MOVED.** A first name beside a grade level and a full assessment with the item detail behind every number are different things. `public/` is empty and still checked; the assessment, the exports and the Backup folder are still excluded and still asserted. |
| **3.86** | Aug 26 | ⭐ **HER DIAGNOSTIC DOCUMENT SAYS WHAT HER RECORD SAYS, AND CHECK #37 KEEPS IT THAT WAY.** §1 carries the settled nine — **Measurement 2.44, Geometry 2.67, Writing Strategies 2.67** — and every row reads *settled* for the first time in this file's life. §1d is recounted across **all 90 answers: 57 read aloud, 63%**. **§1b is untouched**, because it is the Aug 13 baseline and the only thing a growth delta can be measured from. **⚠️ THIRD DRIFT IN THIS ONE FILE.** Aug 24: Grammar 2.15 against a record saying 2.20 — *"the tenth hand-typed number in this project to drift."* Aug 26 morning: Measurement 2.50 against 2.44. **Aug 26 again, one section down: §1d said 54 of her 86 answers when her record held 90** — nobody had mentioned that one at all, it was found while fixing the first. **A rule you have to act on is a CHECK**, the same sentence that produced `check-version-stamp`. **⚠️ ALL THREE STRANDS STILL OPEN ON Aug 25 FINISHED BELOW THE NUMBER THE DOCUMENT PRINTED THAT MORNING** — two by **0.15**, which is three quarters of the entire twelve-day Grammar delta this project calls real growth. §1c keeps the **mid-re-measure column beside the final one** so that is visible rather than quietly overwritten, and **Writing's honest delta is +0.22, not the +0.37 reported on Aug 25.** **⚠️ MEASUREMENT & DATA HAS NOW FALLEN AT EVERY LIKE-FOR-LIKE READING: 2.70 → 2.50 → 2.44.** All three post-extension, all three on the same scale. **Three in a row is no longer a wobble**, on the strand where four of five holes still have no lesson that teaches them. **⚠️ THE CHECK CAN PASS WITHOUT TESTING ANYTHING AND SAYS SO IN WORDS** — both files are kept out of git deliberately, so on Netlify it compares nothing and prints *NOTHING WAS COMPARED*, never the word PASSED. **Gigi's call, made explicitly.** **⚠️ THE YARDSTICK IS THE ANSWER COUNT, NOT THE FILE DATE**, because *newest by mtime* **fails a safer change** — re-saving the stale 86-answer export would point the check at it and turn it red against a **correct** document. Her record only grows. **The file it chose is printed every run.** **⚠️ AND IT REFUSES TO COVER §3**, which is stale as this ships — still 2.50, still 2.82, still arguing the Unit 1 rule **v3.81 overturned.** A check that quietly covered it would make that staleness invisible, so the gap is named in its own output. **NINE NEGATIVE TESTS: four red** — a level drifting, **a strand ROW DELETED** *(an omission must never exempt itself — v3.83)*, the §1d count drifting, and the labels drifting so far that the parser understands nothing and would report **nine silent successes**. **Five green** — §1b changed, §3 changed, the stale export made newest by date, and both absent-file skips. **⚠️ One label was changed in the DOCUMENT rather than loosening the check**: §1 said *Patterns & Algebra*, `strands.js` says *Patterns & Early Algebra*. A fuzzy match would have been the **eighth** assertion here satisfied by something adjacent to the rule. |
| **3.85** | Aug 26 | ⚠️⚠️ **HER WHOLE RECORD WAS ONE COMMIT FROM A PUBLIC REPOSITORY, AGAIN.** Gigi made a **`Backup folder`** to keep her exports in — the right habit — and saved `petal-pestle-backup-2026-08-26.json` into it: **93KB, 99 rows, her name, all nine levels, her journal in her own words.** **None of the four `.gitignore` patterns matched that name** — they look for `her-backup`, `her-latest-export`, `her-*`; hers said `petal-pestle-backup`. **`check-publish-safety` caught it, which is the whole reason it exists** — v3.77 built it to identify an export **structurally**, not by name. v3.73's lesson doing its job three versions later, on a file that was not even renamed: it was simply called something nobody had thought of. **⚠️ So the FOLDER is excluded, not a filename pattern.** A rule that depends on what she names a file fails the first time she names it something else. **And the check asserts two paths by name** — the file she saved, and one she has **not** saved yet in the same folder; **the second is the one that matters**, because narrowing the rule back to one filename would leave the first assertion passing. Negative-tested both ways. **⭐ THE FOLDERS ARE CLEAN.** 17 files to `_to_delete/` — 13 per-version delivery notes superseded by this log, a stale Aug 18 copy of her record, the finished-and-wrong GitHub steps sheet, a v3.40 artifact. **`claude/` went 43 files → 28.** ⚠️ Two live references pointed at moved files and were fixed. **⚠️ A SECOND WRITE-PROBE WAS FOUND, FROM A DIFFERENT SESSION** — `.writetest`, empty, **Aug 24**, two days before the one left in `Lamar DOC`. **Two sessions ran the same pointless test and both left litter they could not remove.** **`RUN-THE-CHECKS.bat` is recorded at last** — built earlier today and never logged. She does not use the command line, so *"run npm run check"* was an instruction she could not follow. **The Quarter 4 reading cap was reviewed and HELD at 14** — §34 said review it after the next Check-In, and **that Check-In did not measure her reading.** Reading Comprehension and Vocabulary have not been asked since Aug 13. **⭐⭐ AND ALL NINE STRANDS ARE SETTLED FOR THE FIRST TIME.** She finished the re-measure Aug 25. **⚠️ All three still running that morning finished LOWER than their mid-re-measure numbers** — Measurement 2.50→**2.44**, Geometry 2.82→**2.67**, Writing 2.82→**2.67**. **Measurement is now her lowest.** Her diagnostic document still calls those three *"not final"* — the first job next session. |
| **3.84** | Aug 26 | ⚠️⚠️ **THIS APP HAD TWO GRADING LADDERS AND THEY DISAGREED.** Gigi: *"i want the grading system to be similar."* Reading Lamar's real `gradeScale.js` made it visible — **`KHAN_LETTER_BANDS` has thirteen bands, `RUBRIC_BANDS` had twelve**, and the missing one was the top. A book report scored 4 on every row came out an **A**; the same 100% on a Khan unit came out an **A+**. **Same percentage, two letters, on one Georgia record.** **It survived because they agreed everywhere anyone had looked** — every band below 97 identical, and her record holds zero writing marks. v3.78 wrote *"two implementations of one metric drift"* and `check-annual-report` asserts the **report** uses one ladder; **nothing asserted the two ladders were one.** That guard exists now and tests **identity plus all 101 percentages**, because a copied table with the same contents today is two tables tomorrow. **⭐ Gigi's call, asked directly: "yes"** — a perfect book report is an A+. It raises a grade on a three-year record. **⭐⭐ AND THE A+ THRESHOLD IS CONFIRMED, NOT ASSUMED.** v3.75 derived the ladder from a screenshot that proved 99 earns an A+ and could not say where A+ **starts**; 97 was used and flagged. His app: `{ letter: 'A+', min: 97, max: 100 }`. **The guess was right**, all thirteen bands match threshold for threshold, and the check now fails if the flag returns **or** the citation is removed. **⭐ ONE BOX, NOT TWO.** Gigi: *"when putting in a fraction for kahn academy the learning app converts the fraction to a percentage and letter grade."* His reason: *"Khan's progress page reports a unit test as 9/11, 8/10, 4/6 — the denominator is not even constant between units."* Accepts and refuses exactly what his does; **a `type="number"` box cannot hold "8/10" at all**, so the feature is one attribute and the check asserts it. **The fraction is still what gets stored.** **⚠️ I did not copy his rubric curve, deliberately** — his `suggestedGradeFromRubric` uses a seven-band curve that disagrees with his own thirteen-band scale. **Hers already does his idea properly** via `1→60, 2→73, 3→87, 4→100`, which came from his log. **⚠️ Two checks were asserting rules she had just overturned** — both **inverted, not deleted**. **⚠️ And three more of my own assertions were satisfied by something adjacent**: one matched `parseScore` in a different function, one counted two inputs within 200 characters when they are hundreds of lines apart, and `parseScore`'s refusals had **no test at all**. **Seven negative tests, all red.** |
| **3.83** | Aug 26 | ⚠️⚠️ **v3.82 BUILT THIS FROM LAMAR'S NOTES WHILE HIS RUNNING APP SAT MOUNTED AND UNOPENED.** Gigi: *"i gave you the newest folder. you arent checking it well."* She was right. `Lamar DOC` is documents only, newest file **Aug 16**; `mission-control-homeschool` is his **actual app**, with `reportFormats.js` updated **Aug 25 22:05**. **Nine days newer, never opened.** §38 committed in the version that quoted §38 — **the second time in eight days.** **HIS REAL FOUR STEPS**, off `assignmentMilestones.js`: Read the book · Notes & structure · Rough draft · Edit & finish. **⚠️ v3.82 got step 2 wrong** — his marking happens *during* week 1, and week 2 turns the marks into **the three or four points the report will make**, which week 3 then writes against. A planning week doing real work, and the earlier version lost it. **⭐ SIX FORMATS, NOT ONE FOUR TIMES A YEAR** — his reason: *"SAME SHAPE FIVE TIMES IS HOW A BOOK REPORT BECOMES A CHORE."* Each with its own sections and checklist, and **the sections ARE the outline**, on screen while she writes. **⚠️ Six, not his sixteen** — Engineering Analysis and Scientific Review are aerospace-shaped, and a table that looks complete while aiming at the wrong child is §35's mistake. **⚠️ HIS 350–500 WORD LENGTH WAS NOT COPIED** — that is a twelve-year-old. Her longest journal entry to date is **eleven words**; hers is 80–150. **All 60 format lines measured** against her Q1 cap, six reworded rather than exempted. **⚠️ THREE BOXES NOW, AND THE LAST STEP GUARDS THE FINISHED ONE.** His writer has notes, draft **and** final; v3.82 built two, so *Edit and finish* could be ticked on last week's unrevised draft — **the checkbox-without-an-artifact bug had moved one box along rather than been fixed.** **The checklist is tickable, and only on step 4.** **⭐ "Gigi asks the questions"** is his Parent Interview, kept because 63% of every answer she has given was read aloud and a written report on a hard week measures handwriting stamina, not the book. **⚠️ AND THE STANDING RULE ABOUT HIS FOLDER CAUSED THIS** — it named `Lamar DOC` and never mentioned his actual app was mounted beside it. Rewritten with both paths. **⚠️ A write test on that read-only folder created a file that could not be deleted** — a rule already written down does not need testing, it needs obeying. **EIGHT MORE NEGATIVE TESTS, ALL RED** — and one **stayed green at first** because the merge fixture had no `final` field: **a fixture that omits a field is a fixture that exempts it.** |
| **3.82** | Aug 25 | ⭐⭐ **THE BOOK REPORTS ARE ON HER SCHEDULE, IN FOUR WEEKLY STEPS.** Gigi: *"Azianna is also supposed to have book reports but I don't see them anywhere... do the book report like Lamar's. Structured so she will know what to do."* **⚠️ THEY WERE THERE ALL ALONG AND NOTHING EVER KNOCKED** — four a year with a frame and a rubric since v3.38, and no schedule, no block, no prompt. **Her record holds zero writing marks**, which is what a door nobody knocks on produces. The inverse of Singing & Movement at v3.64, and the sixth in that family. **⭐ FOUR WEEKLY STEPS, FROM HIS DESIGN**, quoting the source his parent brought: *"Break a single project down into tiny daily pieces instead of asking for a full paper at once... one short paragraph per day."* **⚠️ BUT HIS DATES COULD NOT BE COPIED** — his steps count backward from a real due date and **this app has no calendar and refuses one**, so the mechanism is translated to pace on her progress through Herbalism. Lifting a mechanism out of an app that has something this one deliberately does not is §38 in a new form. **⚠️ A CHECKBOX IS NOT AN ARTIFACT, AND THIS APP WAS IN THAT EXACT STATE**: `writingMarks` held Gigi's marks and **there was nowhere for Azianna to type a word** — four completed reports could have sat on a Georgia record holding nothing she wrote. `db.version(11)` adds `writingDrafts`; the final step cannot be ticked on an empty draft. **TWO BOXES, NEVER ONE** — week 2 marks two places, week 3 writes the draft, and one box means 3 eats 2. **⚠️ AND THE MERGE MAY NEVER LOSE A WORD SHE WROTE**: every other row here takes the newer of two, which is right for a mark and wrong for prose. Longer text wins per field; ticked steps take the union. **⚠️ TWO OF MY OWN ASSERTIONS WERE TOO WEAK** — one used the constant it was testing as its own yardstick, so the expectation moved with the bug. **CHECK #36, TWELVE NEGATIVE TESTS ALL RED.** |
| **3.81** | Aug 25 | ⭐⭐ **EACH MATHS STRAND WALKS ITS OWN UNITS.** Gigi, choosing between leaving Khan's order alone and one track per strand: **"B"**. All three of her maths strands route to 2nd Grade Math and, with no grades recorded, all three were handed **Unit 1**. **⚠️ AND UNIT 1 IS "ADD AND SUBTRACT WITHIN 20", WHICH TEACHES NUMBERS & OPERATIONS — 3.48, her second STRONGEST strand.** Measurement & Data 2.50 lives at Unit 6; Geometry 2.82 at Unit 8. The rule was spending her two weakest strands' half hour on her strongest, for months. Her Measurement block now opens **Unit 5**, the first Measurement unit there is. **⚠️ NOT THE v3.20 BUG COMING BACK, AND THE DIFFERENCE IS ENFORCED RATHER THAN DESCRIBED**: every unit is in exactly one lane, each lane starts at its own first unit and runs in order, and the block still reaches all eight exactly once — **order 5, 6, 7, 1, 2, 3, 4, 8** — before the Course Challenge. **⚠️ FINISHING A LANE IS NOT FINISHING THE COURSE** — the Geometry lane is one unit of eight, and offering the cumulative final there would hand her the finish line for a course she had done an eighth of. **⚠️ SOME STRANDS GET NO LANE AND SAY SO**: no unit of Khan's 2nd Grade Math teaches Patterns & Algebra, and inventing one would aim her weakest-strand time at something that does not teach it — §35's `scale` mistake. Reading gets none either: its units are **themes**, not strands. **⚠️ THE LANES ARE READ OFF KHAN'S TITLES — AN INFERENCE, MARKED AS ONE**, with the name stored beside each lane and a check that fails if Khan renames a unit underneath it. **⚠️ AND `check-khan-units` WAS ASSERTING THE RULE SHE JUST OVERTURNED** — `if (t.unitN !== 1)`, from v3.20 to v3.80. **Inverted, not deleted**, with her words, the date, and the way back. **⚠️ IT WOULD ALSO HAVE GONE ON PRINTING *"all three start at UNIT 1"* ON EVERY RUN** — the assertion was inverted and the claim beside it was not; a check announcing the opposite of what it tests reads as confirmation. **And its own loop was grading the wrong unit** (`unitN: i`, the loop counter), which with lanes would file a result against a unit she never opened. **CHECK #35, TEN NEGATIVE TESTS ALL RED**, the first being the v3.20 bug reintroduced exactly as it happened. |
| **3.80** | Aug 25 | ⭐⭐ **HER READING IS TESTED NOW, AND KHAN NEVER COULD.** Gigi: *"There are no unit tests. How can we test her. In Lamar's app we have passages that he has to read and is tested on it."* She was right and this app already knew — `khanUnits.js`, Aug 16: *"77 links — 49 videos, 15 exercises, 6 articles, **ZERO assessments**... Khan's elementary ELA is themed reading, not a graded course."* The course carries `graded: 'parent'`; the grade was always meant to come from her by hand. **⚠️ AND THE NUMBER THIS EXISTS FOR IS NOT THE PERCENTAGE — it is `readAloud`, per answer.** 54 of her 86 answers were read to her, **63%**, and 5 of her 6 Reading Comprehension answers. Her independent reading **has never been measured**. **⚠️ SO `unaidedPercent` IS NULL AND NEVER 0** — a sitting read entirely aloud reports *no independent reading measured*, not 0%, which would print a nine-year-old a zero for a paper she got every question right on. **⚠️ AND READING THE PASSAGE ALOUD MARKS EVERY QUESTION ON IT**, because an answer drawn from a passage she was read is not independent reading, whichever button she pressed. **She is never told off for using it.** Two passages written here and **measured, not claimed**: 7.5 and 7.6 words a sentence, 0.0% long words, against a Q1 cap of 11 and 6% — deliberately well under, because a first measurement that defeats her tells you only that it was too hard. **⚠️ A READING CHECK IS NEVER A KHAN GRADE** — v3.76's rule from a third direction. **⚠️ HER READING PLACEMENT IS DRIVEN BY VOCABULARY (2.91), NOT COMPREHENSION (3.46)** — Gigi said second grade, a session said third off an eight-day-old export; she was reading her screen. **⚠️ TWO OF MY OWN ASSERTIONS WERE WRONG AND THEIR NEGATIVE TESTS SAID SO**: one matched `isFullyUnaided` and failed correct code, the other required the flag at one of its two write sites. **CHECK #34, ELEVEN NEGATIVE TESTS ALL RED** — and one was rewritten because the first version landed without reintroducing the bug. |
| **3.79** | Aug 25 | ⭐⭐ **TODAY'S PLANNER OPENS THE LESSON, NOT A MENU OF NINETY-SIX.** Gigi: *"I will like it that her today prompt just sends her to the lesson she is to complete and she doesn't see the other lessons. Sometimes she goes ahead without completing the full lesson."* All four app-taught courses resolved to the **course index** — every module, every week, four quarter tabs, nothing locked. **⚠️ AND THE COMMENT ABOVE THE HERBALISM BRANCH HAD SAID OTHERWISE SINCE v2.0:** *"the block that says Herbalism opens the lesson she is up to."* It opened the index. **True about the intention, false about the code, for fifty-nine versions** — and the Khan side got this exact correction at **v3.20** *("her schedule opened a course index, not her unit")* while the app's own courses kept the bug **on the other side of the same file**. **⭐ THE RECORD ALREADY MEANT FINISHED**, which is what made it safe: `markLessonRead` is called only from `finish()`, whose button is `disabled={!allChecked}` — **so a lesson she opened and walked away from was never written down, and it comes back tomorrow.** **⚠️ BUT `db.js` CALLED IT "when she first OPENED a lesson"** — wrong, and load-bearing now: read that sentence and the feature looks like a bug to fix. **A document about an app is not the app**, one sentence from costing the feature. **THE ORDER COMES FROM THE WEEK TABLE, NEVER THE LESSON** — the 13 flat cards `hb-1-01..13` carry no course or week *on the lesson* (§71), so walking lesson metadata would have silently skipped thirteen real lessons; all **256 are now asserted into exactly one week each**, so a lesson added with no week **fails** instead of becoming unreachable *(the "correct and unreachable" failure, five times so far)*. **⚠️ THE WALL IS A DECISION GIGI TURNED DOWN AT v3.63** *("a wall makes a child stop trying")* **and chose here**, for a curriculum rather than a games tab — **written to be inverted with a date, not deleted.** **⚠️ THE HUMAN BODY WAS STILL SAYING "STILL BEING WRITTEN" FOR A 64-LESSON COURSE** — `bodyTarget()` was made derived at v3.46 in **one of the two places that returns it**; the fifth exemption here to outlive its reason by being written down twice. **⚠️ AND `check-links` WENT RED ON A SAFER CHANGE — THE SEVENTH TIME.** Its heading promised *the same course*; its test compared the whole detail string. **Widened to the course segment rather than weakened**, and the v3.42 negative test still bites on both days. **⚠️ MY OWN ASSERTION WAS SATISFIABLE BY THE WRONG STRING:** `disabled={!open}` is a substring of `aria-disabled={!open}`, so deleting the real attribute left the check green and the button clickable — **a lesson that looked locked and opened anyway.** **CHECK #33, `check-lesson-gate`, TWELVE NEGATIVE TESTS ALL RED**, each asserting its own mutation landed first. **KHAN DELIBERATELY UNTOUCHED** — this app cannot see which unit she actually finished, and a wall built on a guess is a locked door with no key. |
| **3.78** | Aug 24 | ⭐⭐ **THE ANNUAL PROGRESS REPORT EXISTS.** **O.C.G.A. § 20-2-690(c)** asks for a **written** annual assessment of progress in **each of five named subjects** — reading, language arts, mathematics, social studies, science — **kept on file for three years**. This app has held every input that report needs for months and **could not print one**. Grown-Up Corner → Report → Annual report. **BUILT TO LAMAR'S SHAPE**, as asked: subject-by-subject letter grades, an **"inside this grade"** breakdown listing each component with its own letter, a **"needs attention"** list, printable — and his note carried across verbatim: *"One grade goes on the record. These are what tell you where to spend the time."* **⚠️ AND THE RULE IT EXISTS TO KEEP IS ONE GIGI FOUND HERSELF, IN HIS APP.** Mission Control's report card printed *"1/106 mastered · 1%"* as though it were a grade, when it measured how much curriculum had been **built**. His log: *"Conflating curriculum completion with performance made the platform's own unfinished state look like his failure."* **So coverage and hours cannot reach a letter here** — they print below the grade, marked separate, and the check **runs the report twice with wildly different coverage and requires every letter to be identical.** **⚠️ UNGRADED IS NULL WITH A REASON — NEVER 0, NEVER F.** Anti-pattern 23, and Lamar's own words. This app had the `Number(null)` -is-0 bug **three times in two days**, once inside a course average. **THE CHECK-IN NEVER TOUCHES A GRADE** — §3.10.6 keeps diagnostic evidence out of mastery; it is reported under **Growth**, on its own instrument, per §3.10.8, and the check proves adding levels moves no letter. **THE HUMAN BODY IS NAMED AS ENRICHMENT AND NEVER COUNTED AS ONE OF THE FIVE.** **ONE LADDER, ONE IMPLEMENTATION** — `letterForPercent` imported, never restated; the check fails if a second ladder appears. **⚠️ AND IT PRINTS "NOT YET GRADED" ALMOST EVERYWHERE TODAY, WHICH IS THE CORRECT OUTPUT AND THE REASON TO BUILD IT IN AUGUST** — 0 Khan grades, 0 writing marks, 0 attempts, 5 lesson reads, 3 school days. **An early-warning instrument, not a trophy.** **SIX NEGATIVE TESTS, ALL RED**, each asserting its own mutation applied first. **NOT TESTED, AND SAID ON EVERY RUN: whether this satisfies Georgia.** Nothing in software can promise that, and none of it is legal advice. |
| **3.77** | Aug 24 | ⭐⭐ **DEPLOYED FROM GITHUB, AND THE CHECKS RUN BEFORE ANYTHING PUBLISHES.** Gigi: *"I want to do this through github to Netlify."* `netlify.toml` sets the build command to **`npm run check && npm run build`**, so **a red check fails the deploy and the old site stays up.** Dragging `dist/` by hand could never do that — **a broken build and a good one dragged identically**, and nothing on the dashboard told them apart. **⚠️ THIRTEEN COPIES OF HER FULL RECORD WERE IN THE FOLDER AND THERE WAS NO `.gitignore` AT ALL** — two in the project, **eleven in `_to_delete/`** left from v3.72's negative tests. `git add .` takes every one. **⚠️ AND GIT IS NOT `public/`.** The v3.72 near-miss was survivable **because a file could be taken back out.** Git keeps history: committing once and deleting in the next commit **does not remove it** — it stays in the repository for ever, in every clone anyone has taken. **The repository is PUBLIC, Gigi's call**, so there is no taking it back at all. **⚠️ `claude/` IS EXCLUDED AND IT IS NOT DOCUMENTATION** — it holds `azianna-diagnostic-results.md`, **a full educational assessment of a named nine-year-old**: every measured level, the item detail behind them, and how often she needed a question read aloud. Her name is across ~30 files there and her raw backup sits beside them. **The master plan and build log go with it, which is a real loss**, and the way to publish them anyway is written in two places that must both change. **`check-publish-safety` NOW ASKS THE GIT QUESTION** — it walks every JSON on disk, identifies her export **structurally** the way it already did for `dist/`, fails if `.gitignore` would not exclude it, **and asserts the specific rules BY NAME.** By name matters: the file scan passes today *because those lines exist*, and would keep passing tomorrow just because a file had moved — **a guard that holds by luck**, which is v3.76's whole lesson. **AND IT ASSERTS THE BUILD COMMAND RUNS THE CHECKS.** **FIVE NEGATIVE TESTS, ALL FIVE RED:** `.gitignore` deleted, the `local/` line commented out, `claude/` commented out, a fresh export dropped in the root under a new name, and the build command stripped to `npm run build`. **⚠️ TWO THINGS I COULD NOT DO ON HER MACHINE, WRITTEN DOWN RATHER THAN GLOSSED:** `_to_delete/` could not be moved out — **this environment permits creating files and not removing them, which is rule 13 enforced by the machine rather than by me** — and **two empty folders I made attempting it are stuck there**, `_to_delete-from-app` and `_archive-test`. Both excluded from git, both hers to delete. **⚠️ AND THE SITE ADDRESS IS STILL LOAD-BEARING** — connect the repo to the **existing** Netlify site; a new one means a new address and her Chromebook opens **empty**. |
| **3.76** | Aug 24 | ⭐⭐ **THE COURSE CHALLENGE HAS A ROW.** Gigi, on how Khan actually works: *"The unit tests are what is being graded by Khan Academy and the course challenge is the test after all the units are completed."* 2nd Grade Math showed **eight unit rows and no ninth** — so **the one cumulative result in the course could not be recorded anywhere**, and would never have reached the annual progress report Georgia asks for. Same entry as a unit: type the fraction, get the percent and the letter. **⚠️ AND IT IS KEPT APART FROM THE UNITS IN BOTH DIRECTIONS.** It can never mark a unit **done** — that would skip a unit she never sat, the thing free-text rows are refused for — and it is never **averaged** with the unit tests, because a cumulative final covers the same material twice and would let one sitting outweigh the eight before it. Her words: *"Both should be recorded, and they are not the same thing."* **⚠️ THE `Number(null)` TRAP, IN A THIRD FILE.** `nextUnitFor` tested `Number.isFinite(Number(g.unitN))`, and **`Number(null)` is 0, which is finite** — so the moment a row existed with no unit number it read as **"unit 0 is done"**. Harmless **only because no Khan course has a unit 0**, which is luck, not design. The same trap was found three times in `khanGrade.js` the same day. **⚠️ AND TWO NEGATIVE TESTS AGAINST THAT RULE CAME BACK GREEN — BOTH RIGHTLY.** Deleting the null guard, and deleting the kind guard, each changed **nothing observable**: the two are redundant with each other and a null unit lands on a unit nobody has. **Seen through `nextUnitFor` alone, every wrong answer the rule can give still yields the right unit.** That is not a check passing — **it is a rule nothing protects, wearing the costume of one**, and the redundancy is defence only while both halves survive. **SO THE PREDICATE IS EXPORTED AND ASKED DIRECTLY** (rule 11): `countsAsUnitDone` asserted on **seven shapes of row**, one at a time, **including the shape the writer refuses to make** — a Course Challenge carrying a unit number, which cannot come from this app but **can arrive in an imported backup**, and is the only case where the kind guard works alone. **The rule lives in two files on purpose** — `khanUnits.js` is the data layer and `blockLinks` imports from it, so importing back would close a cycle — **so all seven shapes are asked of both copies and must agree.** **ROWS FROM v3.74 AND v3.75 STILL WORK, ASSERTED NOT ASSUMED:** a Course Challenge could not be recorded before this version, so **every row without a kind IS a unit test** — a fact about her data, not a convenient default. **⚠️ AND KHAN DISAGREES WITH THIS APP ABOUT WHEN SHE MAY SIT IT, DELIBERATELY** — her screenshot reads **COURSE CHALLENGE: IN PROGRESS** with six units untouched. Khan offers it whenever; this app holds her block on Unit 1 until the units are graded, which was the v3.20 fix for *"maths skips to unit 6"*. **Recording it early is allowed and the row says so; pretending the units are done is not.** **SIX NEGATIVE TESTS, ALL SIX RED**, each asserting its own mutation applied first. |
| **3.75** | Aug 24 | ⭐⭐ **TYPE THE FRACTION, GET THE PERCENT AND THE LETTER.** Gigi: *"Khan Academy scores with fractions. When the fraction is entered by me the app turns it into percentages and letter grades."* A unit row takes two small boxes — **8 of 10** — and shows **"= 80% · B-"** as she types, before she saves. Khan prints a fraction on a unit test; that is what she is reading, so that is what the app asks for. **⚠️ AND v3.74 GOT LAMAR'S LADDER WRONG, HOURS EARLIER, FROM HIS OWN DOCS.** It read `PROJECT_LOG.md` — *"A 90-100, B 80-89, C 70-79, D 60-69, F below 60"* — and **stated it as a fact in this log**. Gigi sent a screenshot of the report card his app **actually renders**: Reading & Literature **A+ 99%**, ELA **A 93%**, Mathematics **B 86%**, Science **B- 82%**. **A+ and B- cannot exist on a five-band scale.** Every point fits the plus/minus ladder and only that. **⚠️ THE RULE THIS BREAKS IS THIS PROJECT'S OWN, FROM A NEW DIRECTION:** *"verify against the disk — and not against the app's own comments either."* **A DOCUMENT ABOUT AN APP IS NOT THE APP.** Lamar's folder holds docs and no code, so **the newest thing in it is still older than his screen.** The ladder is derived from the screenshot now, **all five points asserted**, so it cannot be quietly retuned without one going red. **⚠️ ONE BAND IS A GUESS AND IS MARKED AS ONE** — the screenshot proves A+ exists and that 99 earns it, **not where A+ starts.** 97 is used, affecting 97/98/99 and nothing else, `assumed: true` sits on the band, and **the check fails if the flag is ever removed**: a guess that stops announcing itself becomes a fact. **⚠️ AND THE CHECK SAYS WHAT THE EVIDENCE DOES NOT COVER.** A negative test moved the B- floor 80→79 and **correctly stayed green** — five points prove 82 is a B- and 86 is a B, so **B's floor is only known to be 83..86** and the rungs between are convention. It names what is pinned and what is filled in rather than printing five ticks and letting the ladder look confirmed. **⚠️ THREE NULL BUGS, ALL THE SAME BUG, CAUGHT ON THE CHECK'S FIRST RUN.** `Number(null)` is **0**, `Number('')` is **0**, and **both are finite** — so an absent mark scored 0 and came back an **F**, and **an ungraded unit averaged in as a zero**, which would make an unfinished course look like a failing child. §3.6 already says an unmarked row is not a zero; **it is a guard now, not something to remember.** **THE FRACTION IS STORED, NOT THE PERCENT** — `addWritingMark`'s rule: keep what was observed, compute the conclusion every time it is shown. The row prints **"8/10 · 80%"** beside the letter, **so a grade always shows its working.** **EACH COURSE CARRIES ITS AVERAGE**, the line his report card prints — averaged over what is **recorded**, never the whole course, and reading **"Not yet graded"** rather than 0%. **THE MASTERY WORDS SURVIVE AS THE FALLBACK** (Khan shows a word on a skill), and **`gradedFrom` records whether a grade came from a fraction, a word or an override.** **⚠️ AN UNRECOGNISED OVERRIDE IS REFUSED, NOT SILENTLY DROPPED** — using the fraction instead would record a grade she did not ask for and look like it worked. **⚠️ TWO OF MY OWN NEGATIVE TESTS CAME BACK GREEN AND BOTH WERE THE TEST'S FAULT** — one nudged a band no asserted point could see; the other **replaced a string that was not there, so nothing mutated at all.** *"A mutation that did not mutate"*, #27 on the list, and v3.68 before it. **Redone with an assertion that the mutation applied. SIX NEGATIVE TESTS RED.** |
| **3.74** | Aug 24 | ⭐⭐ **RECORDING A KHAN GRADE NEVER MOVED HER ON, AND IT WAS DIAGNOSED AS "NOT A BUG".** Gigi, Aug 23: *"it is a new week and the links still have the same units connected."* The unit was never meant to follow the calendar — **it follows a recorded result.** The handoff note concluded *"it is not a bug — it is a missing manual step nobody knew was load-bearing"* and queued a hint on Today's Planner as the smallest fix. **That hint would have instructed her to do something that does not work.** `addKhanGrade` stored `{ subject, unit, percent, at, note }`; `nextUnitFor` counts a unit as done **only** when the row carries `courseId` **and** `unitN`. **Neither was ever written by any code path in this app** — one writer, and it wrote neither. **Her record holding ZERO grades is what hid it:** with nothing entered, a broken writer and a working one look identical from the Planner. **⚠️ AND 29 CHECKS PASSED WHILE IT WAS BROKEN.** `check-khan-units` §6c walks Unit 1 → 8 and prints *"advance one unit per grade"* — using grade objects **it built itself**, `grades.push({ courseId: 'math2', unitN: i })`, **a shape the app has never once produced.** It was testing `nextUnitFor` in isolation and making a claim about **the app**. **That same file confesses to the identical mistake at §6b, one section higher** — *"it asserted the block opens an exact unit and never once asked WHICH. It passed, green, while the app skipped five units."* **Twice, in one file, eight lines apart.** **THE SHAPING MOVED INTO `src/lib/khanGrade.js`** (rule 11) — a store action writes to IndexedDB and no check in Node can call it; a pure function can. §6c records through that same writer now, so **a check row and a saved row cannot be different things again.** **THE UNIT IS PICKED, NEVER TYPED** — the free-text box is what made an unmatchable row possible, and the unit name is copied from Khan's own page. **⚠️ Old free-text rows are still IGNORED rather than guessed at** — matching by string would mark a unit she never sat as done, on what becomes a transcript — **and they now sit in their own "Recorded before v3.74" section saying so** instead of looking complete. **AND THE STEP IS WRITTEN DOWN WHERE IT IS DONE:** Khan grades says *"recording a result here is what moves her on"* and lists where all three blocks are open; the schedule panel says *"stays here until you record a result."* **A parent cannot follow a rule nobody told her about.** **⭐ AND THEN GIGI ASKED FOR LAMAR'S GRADING, MID-BUILD — *"exactly like Lamar's. I don't like the dropdown that is currently there."*** **⚠️ AND IT IS NOT THE LADDER THIS APP ALREADY CALLS LAMAR'S.** `RUBRIC_BANDS` in `writingPieces.js` is a twelve-band percent ladder — A 93, A- 90, B+ 87, down to D- 60 — and **its own comment calls it "Lamar's +/- ladder"**. That is his **writing** rubric. **His Khan grading is a different thing**, read off his disk in two files that agree (`PROJECT_LOG.md`, `PROJECT_PLAN.md`): *"a Mark Complete button that reveals an inline A-F grade picker (**A/A-/B+/B/C/D/F**, per the rubric worked out earlier: **Mastered=A, Proficient=A-/B+, Familiar 90-99%=B, Familiar 70-89%=C, below 70%=D**)"*. **Copying the writing ladder onto Khan would have been the wrong answer given confidently** — and it is exactly what "use Lamar's ladder" would have produced without opening his folder. **SO THERE IS NO FORM AND NO DROPDOWN AT ALL.** His Mission Control shows **each skill as its own row**; Khan grades is now rows — every unit of every course in order, the one she is on marked **"she is here"**, and the seven letters appearing beside the row being graded. **AND IT ASKS FOR WHAT KHAN ACTUALLY SHOWS.** The old form wanted a *"Mastery %"* — **a number Khan does not print on the screen Gigi is reading.** Khan says Mastered, Proficient, Familiar. His mapping is printed beside the rows so she is **copying what is on screen rather than converting it**, and **the app never computes the letter**: Khan has no public API, so a figure this app derived would be a figure this app invented. A percent may ride along for the record, and **a missing one is null, not zero.** **SEVEN NEGATIVE TESTS, ALL SEVEN RED:** the writer stops stamping the two fields · the store hand-builds the row again · the form goes back to free text · the ladder is quietly cut to a plain A-F, dropping A- and B+ · the writer stops refusing a letter that is not on the ladder · the picker is replaced by a dropdown · and below-70% starts telling her she is *"nearly there"*. **⚠️ AND HER DIAGNOSTIC DOCUMENT HAD DRIFTED FROM HER RECORD** — `azianna-diagnostic-results.md` said Grammar **2.15**, Writing **2.45**, Geometry and Measurement **2.00**; her record says **2.20, 2.70, 2.70, 2.70**. **The tenth hand-typed number in this project to drift, in the one document §30 makes govern the reading level of all 256 lessons.** Corrected from her backup with the **Aug 13 baseline kept beside it** — §3.10.8 measures growth from it. The four that moved are the four still `settled: false`. **STILL NOT BUILT:** Unit test vs Course Challenge. `addKhanGrade` has **no field for which kind of assessment a result was**, and `nextUnitFor` treats any recorded result as *"unit done"*. **Both should be recorded and they are not the same thing** — it is the one remaining Khan job, and the only one that changes what a grade MEANS. |
| **3.73** | Aug 23 | ⭐⭐ **THE WINDOWS RUN FINALLY HAPPENED, AND 18 OF 30 CHECKS HAD NEVER WORKED THERE.** Gigi ran `npm run check` on her own machine for the first time: **12 of 30 passed in THREE SECONDS.** Thirty checks cannot run in three seconds — **they were crashing on startup, not failing on content.** **THE CAUSE: seventeen scripts do `await import(resolve(ROOT, …))`.** On Linux an absolute path is a usable module specifier. **On Windows it is not** — node throws `ERR_UNSUPPORTED_ESM_URL_SCHEME`, because `C:\…` is not a `file://` URL. Every one died before its first assertion. **Fixed with `pathToFileURL(…).href` in all seventeen.** **⚠️ THIS IS THE BUILD LOG'S OWN CAVEAT COMING TRUE.** Every run-status line has read *"green against the real folder — **but on LINUX**. Windows is the run that confirms it, and it has not happened yet."* It has now, and the answer was that **more than half the suite was decorative on the machine this app is developed on.** Nothing was wrong with the content they test; they never got to look. **⚠️ AND `check-publish-safety` FAILED ITS FIRST REAL BUILD ON FOUR FILES, ALL CORRECT.** It scanned for the words `strandStates`, `learnerName`, `reviewItems` — and `dist/assets/index-*.js` is **the compiled app**, which is full of them, because they are **its own table and field names**. *The check was reading the code that HANDLES her data and calling it her data.* **Fifth time this project has had a check fail correct content, and the worst possible one to have crying wolf.** Rebuilt to identify an export **structurally** — parses as JSON and says `app: Petal & Pestle Academy` — which compiled JavaScript cannot do whatever words are in it. **SEVEN NEGATIVE TESTS, six red and the seventh INVERTED and correctly green:** her export in `public/`, in `dist/`, **renamed to `data.json`** (structural catches it, a word scan would not), both guards deleted, the SOURCE `index.html` uploaded by mistake — and **her real build, which must PASS and does.** **HER BUILD IS CLEAN:** `dist/` holds `index.html` and 19 assets, points at `assets/` not `/src/`, carries none of her data. **That is the folder to drag to Netlify.** |
| **3.72** | Aug 23 | ⭐⭐ **HER DATA WAS ONE UPLOAD AWAY FROM BEING PUBLIC, AND NOBODY HAD DONE ANYTHING WRONG.** Moving Azianna to a Chromebook means publishing the app to a web address. **`public/her-latest-export.json` is her real export** — learner name, **all nine measured strand levels** (grammar 2.20, geometry 2.70, reading 3.46), **74 answers, and both journal entries in her own words.** **Everything in `public/` is copied verbatim into `dist/`,** so the first drag-and-drop to Netlify would have put it at a readable URL. **While the app only ever ran on localhost, `public/` and "on my own computer" were the same place.** Publishing is what made them different, and the file had sat there since August with no reason to think about it. **THE FIX KEEPS THE FEATURE.** The file moved to `local/`, which Vite does not know about, and a dev-only Vite plugin (`apply: 'serve'`) hands it to the dev server at the same address the Grown-Up Corner already asks for. The panel's fetch is wrapped in `import.meta.env.DEV`. **The green "Load her latest export" button still works on Gigi's Windows machine and cannot exist in a build.** *"Or open a file yourself"* is untouched — that is how the backup gets loaded on the Chromebook. **⚠️ AND MY OWN CHECK FAILED ITS NEGATIVE TEST FOR THE REASON THIS LOG ALREADY NAMES.** Both guard assertions searched the raw source, and **`vite.config.js` explains `apply: 'serve'` in a comment while the panel explains `import.meta.env.DEV` in a comment.** Deleting the real line left the words behind and **the check stayed green twice.** That is v3.62 — *"a check that greps a component instead of calling it"*, one of which *"matched the comments explaining the decision it was testing"* — arriving again in a new file. **Fixed with `codeOnly()`, the same answer as last time.** **FIVE NEGATIVE TESTS. Four red, and the fifth INVERTED and correctly green:** her export back in `public/`, a stale `dist/` already carrying it, the plugin guard deleted, the fetch guard deleted — and then **comments still naming both guards while the code is intact, which must NOT fail.** **AND IT ANSWERS THE QUESTION A HOST ASKS FIRST: WHERE IS index.html.** This project has TWO. `index.html` at the root is the SOURCE template and points at `/src/main.jsx`, which no browser can run; `dist/index.html` is written by the build. **Drag the project folder to Netlify and it is ACCEPTED** — an index.html is there — **and then serves a blank page.** It looks like the upload worked. `check-publish-safety` now says whether a `dist/` is uploadable: index present, not the source template, and referencing real built assets. **Five more scenarios tested — no dist, the source index copied in, a dist with no index, a good build, and a good build still carrying her data.** **⚠️ NOTHING WAS DELETED.** Two stray test copies were moved to `_to_delete/`. **The build itself could not be run here** — `rollup` and `esbuild` ship Windows-only binaries in this folder and the npm registry is unreachable from the sandbox — so **`npm run build` is Gigi's one command**, and `check-publish-safety` is what makes it safe to upload what comes out. **All 30 checks green.** |
| **3.71** | Aug 23 | ⭐ **THE SKILL CATALOG IS FROZEN AND ON DISK — 156 CANDIDATES CUT TO 20.** §3.2: *"Skills — not lessons — are what get mastered, reviewed, remediated and reported on."* Derived off the `words:` lists — **1,024 entries, 801 unique**, of which **645 appear on exactly one lesson and are therefore vocabulary, not skills.** **THE CUT RULE HAS THREE PARTS AND THE THIRD DID MOST OF THE WORK: a term is not a skill if a Georgia standards code already says it.** The 61 codes cover 117 lessons in the terms a report to Georgia asks in, so a skill named `pollination` beside `S4L1a` is one claim written twice and the second copy is maintained by hand for ever. That removes almost all of the within-course list and most of the cross-course one. **What survives is what the codes cannot say: how she works (11) and how she measures (9).** **⚠️ `scale` WAS TAGGED AS A MEASUREMENT SKILL AND IS NOT ONE** — retired on the evidence of the Science Lab Q3 blueprint, where it means **scale MODEL**: lesson 40 *"A model where the DISTANCES are right"* (*Toilet Paper Scale*) and lesson 41 *"Why no model can do both at once"* (*To Scale: THE SOLAR SYSTEM*). A Measurement & Data game set built on it would have been aimed at the wrong child. **THREE MORE ARE HELD, NOT GUESSED IN** — `balance` carries a Social Studies lesson and SS4CG is checks-and-balances territory; `volume` may be loudness in a course that carries `sound`, `pitch` and `vibration`; `shape` may be moon PHASES in the moon-and-orbit course. **Each carries the check to run, which is the one already run on `weight`: list the carrying lessons, read the titles.** `check-skills` fails if a held term is added to the catalog without being removed from the held list, so the check cannot be skipped by accident. **⚠️ TWO MERGES CARRY `lessonCount: null` ON PURPOSE.** `prediction` folds in `predict` and `forecast` — 3 + 2 + 4 = **9 lesson slots that may sit on the same lessons**, so the true number is between 4 and 9. **Printing 9 would be the app overstating its own coverage**, which is the thing §5.4 exists to prevent, so the field is null with the reason written in and the check FAILS any null that has no reason. `controlled-variable` folds in `variable` and `control group` and **deliberately does not fold in bare `control`**, which means NERVE control in the Human Body — merging it would drop nerve lessons into a fair-test remediation set. **The rename is what makes that mistake unavailable later.** **⚠️ AND THE MEASUREMENT BRIDGE BUYS UNITS AND ONLY UNITS.** Her Aug 13 Check-In named the holes precisely — units 0 of 3, **perimeter 0 of 3, area 0 of 2, telling time 1 of 3, elapsed time 0 of 1**. Four of the five have **no skill to hang a game on**, because no lesson in the app teaches them. **Geometry has exactly ONE skill behind it** — `angle`, 2 lessons, 20 questions — and if `shape` fails its check that is the entire geometry bridge. Recorded in the data layer as `PHASE_4_SCOPE` because it is easy to read *"9 measurement skills"* and believe a score-targeted game will aim at her weak strands. **Not a reason to stop. A reason not to promise it.** **⚠️ AND 13 LESSONS WOULD HAVE BEEN TAGGED WITHOUT KNOWING WHAT COURSE THEY ARE IN** — `hb-1-01` to `hb-1-13` carry `words:` but no `course`, `quarter`, `week` or `standards`. **Fix before the Phase 2 tagging pass, not after.** **NINE NEGATIVE TESTS, ALL NINE RED**, including a held term made live, a retirement quietly undone, and a skill on one lesson. **All 29 checks green** — and `check-version-stamp`, one version old, **caught its own successor arriving**, failing on 29 scripts against a header that still said 28. |
| **3.70** | Aug 23 | ⭐ **THE LESSON PROSE IS MEASURED — ALL 256, FOR THE FIRST TIME.** `check-readability` has always asked whether she can read a diagnostic ITEM. **Nothing asked whether she can read a LESSON**, and the lessons are what she spends the year inside. At v3.40 a whole writing programme was caught pitched years above her **by reading it**; the mini-lessons were measured after that and **the 256 course lessons were not.** **⚠️ THE CAPS WERE REAL AND NOTHING TESTED THEM** — written in **seven module header comments across fifty-six lesson files** (`humanbodyM1.js:33` *"Quarter 1 cap: 11 words per sentence"*; `socialM9.js:29` *"Cap 14 words a sentence, FLOOR 6.5, long-word cap 10%"*). **That is the same failure `check-readability` exists for** — its own header says the first `elaItems.js` *"CLAIMED every passage sat at or below the grade it tested. That claim was asserted in a comment, never measured."* **The claim moved into the lesson files and stayed a comment.** It lives in `src/lib/readingCaps.js` now, where a check reads it. **ONE METRIC, TWO CHECKS** — `analyse()`, `syllables()` and both word lists extracted **verbatim** into `src/lib/readingLoad.js`. Two copies of *how hard is this to read* drift, and the day they disagree you have two numbers that cannot be compared. **The extraction was proved: output captured before and after and diffed BYTE FOR BYTE, identical.** **⚠️ PROPER NOUNS ARE EXEMPT, AND THE EVIDENCE WAS BLUNT.** Without the allowance Social Studies failed **eighteen** lessons for *Charleston, Carolina, Confederate, Anthony, Galileo, Jupiter* — and `body-m16-03` failed for the name **Alexa Canady**, the first Black woman neurosurgeon in the United States, in the course written because Azianna wants to be a doctor. **You cannot shorten a name.** Gigi: *"Proper nouns stays. It does not make any sense to learn about a person without their name."* Same pressure `SUBJECT_TERMS` already prevents. **Defaults OFF so the item bank is untouched;** the lesson check opts in. `COMMON_WORDS` extended for narrative — `hb-1-08` scored **16.7%** on the strength of *every* and *visitor*, because the counter reads *e-ve-ry* as three syllables. **Long-word failures fell from 27 to 3, and all three are real.** **IT PASSES TODAY WITH 37 BREACHES RECORDED AS DEBT, AND THE LIST IS A RATCHET.** A check that fails 37 times on arrival teaches you to run the batch with one red line you ignore. `KNOWN_OVER` holds them dated and reasoned, and the check asserts the list is **exactly right in both directions** — a new breach fails, **and a lesson no longer over fails until its line is deleted.** The debt can only go down and cannot grow quietly. **⚠️ HERBALISM M13, M14 AND M15 ARE EIGHTEEN FOR EIGHTEEN** — every lesson in all three, **16 to 19.6 words a sentence against a cap of 14**. The oldest content in the app, written at v3.9 **before the ramp policy existed**. M16 next door is clean; so is every Human Body Q4 module. **DECISION: split the sentences, do not raise the cap** — to let them stand you would have to set Q4 at **twenty words a sentence**, lowering a standard the other 240 lessons already meet. **A splitting job, not a rewrite of what is taught. Not urgent: Q4 is April.** **⚠️ QUARTER 4 HELD AT 14, RECORDED AS A DECISION RATHER THAN A MEASUREMENT.** Nothing on disk ever stated a Q4 cap — the ramp runs 11, 12, 14 and stops, because Q4 Herbalism predates the policy. §3.10.8: *"growth is a delta on ONE INSTRUMENT’S SCALE"*, and this app has one. Her baseline came off the Check-In on Aug 13 and **no second sitting is scheduled**. **The weekly and quarterly tests cannot answer it** — they carry `evidenceSource: 'test'` and draw from course banks with **no strand**, and §3.10.6 keeps that wall up on purpose. **Review the number after the next Check-In.** **⚠️ THE 13 FLAT CARDS ARE WORSE THAN FLAT** — `hb-1-01`–`hb-1-13` carry **no `course`, no `quarter`, no `week`, no `standards`**, so no cap applies. Listed as `UNCAPPABLE` and reported, **never silently skipped; the check fails if the list grows.** **They do carry `words:`**, so the Phase 2 skill tagging would tag thirteen lessons that do not know what course they are in. **Fix before the tagging pass, not after.** **⚠️ AND MOVING THE METRIC INTO `src/` TRIPPED A BLIND SPOT IN `check-sources`.** `blankNonCode()` strips comments and strings before counting brackets and has **no concept of a regex literal**, so a quote inside a character class opens a string in its parser, swallows the closing bracket and reports a phantom unbalanced bracket. **It never bit before because `check-sources` scans `src/` only and this code lived in `scripts/`.** Worked around with `\u0027`/`\u0022` escapes, written down at the top of the file. **Same family as the curly-apostrophe rule for JSX. The blind spot itself is not fixed.** **Four negative tests, all four red:** a lesson removed from `KNOWN_OVER` while still over, a passing lesson added to it, a flat card dropped from `UNCAPPABLE`, and real prose lengthened in `body-m1-01` until it breached. **All 27 checks green** — the new one auto-discovered by `run-all-checks` with no edit to it. |
| **3.69** | Aug 19 | **⭐ THE PANEL COUNTS HER SENTENCES — AND DOES NOT GRADE THEM.** Gigi: *"Is there a grader in the journal entry that will grade her work?"* **No, and the standard forbids one:** §3.6 says `freeResponse` is **"never auto-scored"**, and Edition A is *"No Internet, No AI Service."* So it **counts** — capitals at the start, marks at the end, how many sentences — **suggests** a level, and Gigi accepts or overrides. *Engine proposes, adult approves.* **⚠️ Row 2 only**, and `SUGGESTIBLE_ROWS` is exported so a check asserts the list rather than trusting a comment — *"answered the question"* and *"said enough"* need reading what she meant. **⚠️ And the first draft would have systematically over-graded her:** it banded on the **raw slip count**, so a one-sentence entry with nothing right scored **3** — descriptor *"one or two slips."* Her entries are 8, 11 and 9 words. **Caught by running it against her three real entries before wiring it to anything.** Bands on the **proportion** now; her three read 2, 1, 2. **Empty returns null, not 1.** **5 negative tests, all red.** |
| **3.68** | Aug 19 | **⭐ THE DAILY JOURNAL IS MARKED, AND SHE SEES IT.** Gigi overturned her own locked rule — offered a separate graded daily piece she said *"she isn't going to want to do the daily writing and the journal."* **Three rows** — answered the question · **sentences and punctuation** · said enough. **Row 2 is the point:** grammar is her lowest strand at **2.20**, no course teaches it, and this is now the only place in her week an adult looks at her sentences on purpose. **Same ladder as the book reports** (all 3s = 87 = B+), percents averaged not summed — the v3.56 defect cannot return. **An unmarked row is not a zero** and an unmarked entry has no row at all. **⚠️ Her writing is never touched** — `journalMarks` is its own table at **db.version(10)**, keyed by entryId, so a bad merge can lose a mark and never a word. **⚠️ The old rule is INVERTED, not deleted**, carrying her words and the date. **Two locks did not move:** the mini-lesson still bans an input, and *"Say something back"* is still feedback that fails on a score — the mark lives in its own component so neither erodes. **⭐ The kind is assigned by weekday now**, derived from `JOURNAL_KINDS`. **⚠️ One negative test passed wrongly** — the mutation declared `setKind` and never called it, so it never restored the bug. Redone. **9 negative tests, all red.** |
| **3.67** | Aug 19 | **⭐ TWO-WAY EXPORT — and it always worked.** Gigi: *"I left a message on her journals. Where would she see them?"* **⚠️ The panel said something false:** *"a note written here will never reach her."* `exportAll` ships messages, `importBackup` merges them, and a fresh note reaches a machine that has never seen it — proven by a check passing since v3.44. Nobody had used that direction, so the screen **wrote the habit down as a limit** and cost her a feature the app already had. **⚠️ And my own guard was eating the note:** a file carrying a note is, from her laptop's side, older with **no new answers**, and staleness counted answers only — so it was refused with the note inside it. **Third time in one day the load guard was wrong about a file it was protecting.** Staleness now means **adds nothing at all**, counted by walking every table the preview reports. The screen now gives **the steps**, and a check fails the build if the false sentence returns or the steps go. **⚠️ One of my fixtures described a file that cannot exist** — 14 new answers and 0 new rows, two numbers `previewImport` computes once. **5 negative tests.** |
| **3.66** | Aug 19 | **⚠️ THE MORNING'S FIX WOULD HAVE THROWN AWAY THE RE-MEASURE IT WAS WRITTEN TO PROTECT.** Same rule, both directions, twelve hours apart. **Morning:** higher `asked` won, and a re-measure restarts the count, so the Aug 13 file (asked 8, settled, **2.00**) beat the in-progress Aug 18 reading (asked 3, **2.70**). Fixed: in-progress beats finished. **⚠️ Evening — Gigi: *"she worked today."*** Azianna **finished** the re-measure, so incoming is asked 12 and **settled** while local is still the asked-3 stub — and the morning's rule **kept the stub and discarded the completed measurement.** Caught before she loaded the file. **"In progress" was never the signal** — it is a proxy for *more recent*, good only while the re-measure is unfinished. **`seenItemIds` is the real one:** the Aug 13 reading and the re-measure share **not one item**; a file taken after she carries on holds those same ids **plus more** — the same reading carried further, and it must win. Order now: **continuation → in-progress → count**, all three holding at once. `previewImport` now **asks** `pickStrand` instead of copying its rule. **⚠️ And one of tonight's own assertions was worthless** — the empty-`seenItemIds` guard was tested with a fixture that gave the same answer either way; rewritten to the shape that bites. **4 negative tests.** |
| **3.65** | Aug 19 | **⭐ BLOOKET, GIMKIT AND KAHOOT — a NAMED exception, not a loosened rule.** Gigi asked for all three by name; they collide with the bar she set the same afternoon, and she chose to **declare** them rather than waive it. Way back: drop three entries. **⚠️ They link to the DOOR, not the shop.** All three marketing pages were opened: `blooket.com` is a Sign up button, `gimkit.com` shows *"Group Pricing"* twice, and **`kahoot.com` is a full storefront — $3/mo, $19/mo, $19/mo, "Save 20% until August 23", "Contact sales"** in front of a nine-year-old. So the **join** pages are used — `play.blooket.com/play`, `gimkit.com/join`, `kahoot.it` — which removes the pricing entirely for two and reduces Kahoot to one line with no figures. A check fails the build if any is pointed back at its homepage. **⚠️ And the real problem was never the pricing:** all three need a **host code**. Everything else in the tab is *open it and play*; these are *join a game someone is running*. They carry `kind: 'hosted'` and their cards read **"ask Gigi for a code first"** — asserted in the data *and* on the rendered card. They **sort last on their own**, because the tab orders by weakest measured strand and these teach none. **5 negative tests, all red.** |
| **3.64** | Aug 19 | **⭐ SINGING & YOGA — AZIANNA ASKED FOR IT HERSELF.** Twelve videos, two ladders of six, exactly as **§9** specified when queued as item **7b at v3.2**. Both channels are **Gigi's own picks** from Aug 14 — Cheryl Porter, a Black American vocal coach from Detroit, and Yoga Guppy. **⚠️ The 15:40 block has existed since v3.2 and opened nothing** — a tick-box with no door for sixty-one versions, the **fifth** correct-and-unreachable thing in this app. **⚠️ And the one-word fix would have put 15 minutes a day into a legal record:** `resolveBlockTarget` needs a `subject`, and `minutesOf()` counts a block toward the **Georgia instructional total** only when it has one. Matched by id above the guard; a check now fails the build if `blk-doing` is ever given a subject. **Every video verified three ways** — oEmbed with the title character-for-character, length read not estimated, and **played in an iframe on the app's own origin**. **⚠️ That last test nearly threw out all six yoga videos:** a top-level nocookie load returns **Error 153** for videos that embed fine — *rejecting a good video is the mirror of accepting a bad one.* A fake-id control returned **400, not the empty body** this project had recorded. **Embedded, never linked** — the watch pages served a pre-roll ad on every one. **Never graded**, and this screen may honestly say so. **⚠️ One rung flagged for Gigi to watch** — a food-themed video not yet judged against the Q3 fence. **8 negative tests, all red.** |
| **3.63** | Aug 19 | **⭐ A PLAY TAB — eleven verified links for when the lesson is done and the day is not.** Gigi: *"When she completes a lesson early she can do those instead of walk away."* **Never locked — her call**, the day's remaining work is *said*, not enforced; a check fails the build if anything there becomes disabled, written to be inverted rather than deleted. Weighted toward her four weakest strands and **never naming them to her** — the order carries it. **⚠️ Eleven links produced FIVE wrong addresses in one afternoon:** four Cyberchase games guessed from tile names (**all four 404** — the real pattern is `/games/play/<slug>/<id>`), a guessed RoomRecess path (404), `wordgirl/games` **silently redirecting to `/videos/`**, and `apps.mathlearningcenter.org` silently redirecting. **⚠️ And the first draft claimed "every address was opened" while 8 of 11 were inferred** — the RoomRecess mistake, made inside the file recording the RoomRecess mistake. **⭐ New standing rule from Gigi: a payment prompt on the page rules a site out** — it excluded RoomRecess *and* **Black Education Station**, the one Black-educator result found, kept in REJECTED rather than quietly included. **PBS Kids' main page rejected for being Elmo** — the one place her age outranks her level. **⚠️ Grammar 2.20 is empty and declared**, never silently absent. **⚠️ My own check failed correct content twice** — it matched the comments explaining the decision (**rule 21, fifth time**), then the fix sliced at an offset from the unstripped text. **8 negative tests + 1 inverted.** |
| **3.62** | Aug 19 | **⭐ THE RETRIEVE PANEL NAMES THE LESSON ITS QUESTIONS CAME FROM.** Gigi, on Social Studies lesson 2: *"Why would there be questions about something she hasn't learned yet?"* **She had learned it** — `ss-m1-01-q1` belongs to Lesson 1, read Aug 17, whose first beat says *"Britain and France fought for years over land in America."* **The beat was right and the screen was unreadable.** *"From earlier in this course"* is vague enough that the one adult who can audit this had no way to check it. Now *"from Lesson 1"*, **derived from the questions shown, never typed.** **⚠️ The video did not move** — *"The video can stay. I just didn't understand where the questions were coming from."* **⚠️ And I shipped a crash into the file she opens every morning:** `lessonById()` called in `LessonReader.jsx` and never imported — every retrieve-beat lesson would have white-screened **with all 26 green**, because `check-imports` only ever asked whether an import points at a real export. **It asks the other half now.** ⚠️ Its first run **failed six times on correct content** — changelog prose quoting real function names — **fixed at the cause with `codeOnly()`, not by exempting the file.** Blind spot written down: template interpolations are missed, a false negative chosen over a false positive. **Four negative tests, the fourth inverted.** |
| **3.61** | Aug 19 | **⭐ A LESSON OPENS WITH WHAT SHE ALREADY KNOWS NOW, NOT WITH NEW TEACHING.** §3.2's ladder is **retrieve → teach → check → teach → check → practice → apply → test**, and this app started at `teach` for its whole life. Two questions from earlier in the same course sit above the hook and the video. **⚠️ It is here because Gigi overruled the plan** — offered beat *timing* (the last open Batch A row) she said *"Isn't this the beat that will assist with retention and learning? I don't want any short cuts."* **Timing measures her; the ladder teaches her.** It **calls `pickWarmUp`** rather than reimplementing it — one retrieval policy, never two, because a second picker would start identical and drift invisibly. **Never asks about the lesson it opens** *(the oldest rule in the project does not lapse because the asking moved to the top of the page)*, never serves what she answered today, **renders nothing on the first lesson of a course**, and **never gates**. It is recorded and **says so** rather than repeating the v3.56 lie. **Read aloud** via the warm-up's own `chunksForItem()` — her Reading 3.46 is a listening score. **⚠️ Two of my own assertions were wrong:** one anchored on a field name whose first hit is inside the read-aloud function *(rule 21, fourth time)*; **the other did not bite at all** — its fixture let the picker walk past the bug, and it passed on broken code until assertion 4 was removed to make it speak for itself. Fixture now stacked against the rule. **Eight negative tests, all eight red.** |
| **3.60** | Aug 19 | **⭐ LOADING HER AUG 13 BACKUP WOULD HAVE THROWN AWAY ALL FOUR RE-MEASURES — read off her two real files, not theorised.** `pickStrand` keeps the higher `asked`, and **a re-measure starts the count over**, so Aug 13 geometry (asked 8, settled, **2.00**) beats Aug 18 geometry (asked 3, running, **2.70**). Four for four. **⚠️ And the reverted rows come back `settled: true`** — the flag the goals engine reads — so the four strands it deliberately refuses would have become eligible, and a year target could have been set on **2.00, the item-bank floor that was never a measurement of her.** The rule now: **a reading still being taken beats a finished older one**, either direction; same-kind readings still go on count; **an in-progress row with zero answers is not evidence.** **⭐ The green button refuses and the picker warns** *(Gigi's call)* — and an older file is **not** refused for being older, only for being older **and** bringing nothing. The strands are **named, not counted**. Decision lives in `lib/importGuard.js` and `check-import` **calls** it. **⭐ Six exchanges to find the export control, and the cause was one missing word** — the button says **Export / download backup** now. **⚠️ A check caught that rename and was fixed rather than loosened:** it held a hand-typed copy of another panel's label; it **reads** it now. **⭐ The read-aloud caveat covered half the strands it applies to** — Grammar and Writing were missing from a list of two typed where no check could reach it, and **`readAloudByStrand()` has rendered nowhere since v3.55.** Both fixed. **17 negative tests across three checks, all catching, and one inverted** — a correct rename of both label sites must stay green, and does. |
| **3.59** | Aug 18 | **⭐ THE GOAL IS ON A SCREEN NOW, BOTH SIDES.** §3.11.6 — *"a goal appears as one line on the today surface or it does not exist."* An engine with no screen would have been the **fourth** time this app built something correct and invisible. **Her side:** one line above her day — where she started, how far she has moved, days left. **No progress bar until she has actually moved.** **⚠️ And her screen deliberately hides the verdict** — *"Out of reach"* is a planning word for an adult; on a child's home screen every morning it tells her the year is lost before she starts. A check fails the build if it ever appears there. **Gigi's side:** target and deadline are pickers, so May 26 vs Aug 1 is one click and 26 school days; **the verdict renders before the save button**, per §3.11.4. The four refused strands are shown **with their reason**, not hidden. **⚠️ Two of eight negative tests failed first, the same way, again** — one assertion found the component's own *definition*, the other a filter at the top of the file. **Third version running that an assertion was satisfied by a name rather than a use.** See §3 entry 21. |
| **3.58** | Aug 18 | **⭐ THE APP HOLDS A LEARNING GOAL NOW, AND IT IS GIGI'S.** *"I want her caught up to 5th grade by the end of the school year"* — then the question that changed the answer: *"What if the goal is before the beginning of the next school year?"* **Moving the deadline from May 26 to Aug 1 adds the summer term's 26 school days and takes FIVE of nine strands from Out of reach to Stretch.** A structural decision made in July is what makes a goal set in August answerable. **One goal per strand, never one for everything** — the same 5.0 is Reachable in fractions and Out of reach in grammar. **⭐ Four strands are REFUSED a goal on purpose:** grammar's 2.20 rests on 4 questions, geometry/measurement/writing on 3 each. *A target set from a number like that and missed next July is not a measurement — it is an accusation.* **⭐ The school calendar is DATA now (§3.12.1)** — 216 days lived only in a markdown table; the hand estimate of "213 days left" was **seven days out**. **Database v9 — the export guard caught the new table again**, second schema change in two versions. **Two merge rules that are deliberate opposites: a baseline never moves, a goal is meant to.** **Nine negative tests, all nine caught.** |
| **3.57** | Aug 18 | **⭐ BATCH A — every answer now says where it came from, and the warm-up finally keeps a record.** Database **v8**, two new tables: `itemEvents` (one row per answer, carrying `evidenceSource`) and `baselines` (one row per course, written once). **⚠️ And the audit was wrong about the thing Batch A was sized on** — it said an attempt holds 4 fields; it holds **14 plus a per-question `rows[]`**, and every test she has sat already recorded which questions she got wrong. **The warm-up was the real hole:** three questions a morning, 260 mornings a year, and the only trace was a Leitner box moving — and it is the *strongest* evidence in the app, retrieval days after the lesson rather than recognition ten minutes after. **7 sources, 4 counting toward mastery — the Check-In is not one**, because *"a wrong answer on untaught material is information, not a grade."* `test` is a **declared extension**, written down with its reason. **`attemptState` is not hypothetical — her backup holds three abandoned sittings.** **A baseline is written once and the EARLIER one wins a merge** — the only rule in this app that prefers the older side. **⭐ And the export guard written this morning caught both new tables on its first real schema change**, at all four links. **⚠️ And this session's check made the same mistake a fourth time** — appended *after* the block that prints errors and exits, so it ran and reported to nothing. See §3 entry 20. **Eight negative tests, all eight red.** |
| **3.56** | Aug 18 | **⭐ §32.1 IS CLOSED.** **The backup screen was showing less than half of what the button does** — `importBackup` merges **15** tables, `previewImport` described **11**, the screen listed **7**. Three hand-maintained lists, none agreeing, and the four never mentioned included **her projects, her Khan grades and her graded writing.** Rows are **derived** from the preview now; an unlabelled table shows under its raw name. **⭐ And the version stamp is finally read** — `previewImport` gated on `data.app` alone for seven versions while the export stamped a version with a comment saying why it mattered. **New-into-old is refused** with a sentence a person can act on; old-into-new stays safe, because her Aug 13 file is `version: 2` and still holds real work. `BACKUP_VERSION` is one constant, asserted against the schema. **⚠️ And this check broke itself twice and said nothing both times** — see §3 entry 19. **⭐ AND GIGI FOUND THE SECOND LIE FROM A SCREENSHOT — the eleventh bug she has found by using the app.** The practice gate said *"That is not a problem **and it is not a score**"* three inches below the line just corrected to say the opposite. *Not a problem* is true. *Not a score* is not — `markLessonRead` keeps `passed`, and `shakyLessons()` reads it. **The false half is gone; the kind half stays. ⚠️ And the obvious fix would have been wrong** — banning the phrase file-wide fails **correct content**, because Try It Now says *"not scored"* and **means it**, writing nothing anywhere. **Scoped per panel instead:** a panel that writes nothing may say so; a panel whose result is recorded may not. **Five negative tests, and the fifth is the one that matters — ApplyIt keeps its honest sentence and the check stays green.** **⭐ AND SHE NOW FINDS OUT HOW SHE DID, AT THE MOMENT SHE DID IT.** The finish screen printed **one message, identical for 3 of 3 and 1 of 3.** On `sl-m2-01`, Aug 17, she got two of three wrong and the app said nothing and moved her on. Gigi: *"Should it be only weekly for a child that is behind in her levels?"* **No.** Four outcomes, four different things said, and **a miss hands the question back** — the question, the right answer, the line saying why hers was wrong. Warm, never softened; **no praise over a miss.** Read aloud, because her Reading 3.46 is a listening score. **⚠️ AND THE SCREEN WAS LYING** — *"Nothing is written down. This is just for you."* while `markLessonRead` wrote `asked`, `correct` and `passed` into her backup and the Gradebook. **A screen may not tell a child something is not recorded when it is**, and the build now fails if one does. **⭐ THE FINISH BUTTON IS GATED ON ANSWERING** — `practiceGateResult([])` returned `passed: true`, so a lesson finished with nothing answered was written down as passed. **Not the wall Gigi ruled out: any answer opens it, a guess is fine. It is a gate on the record, not on the child.** **⭐ MEETING THE STANDARD ON EVERY ROW OF A RUBRIC WAS A C. It is a B+ now.** `gradePiece()` used `total / (rows × 4) × 100`, so all 3s = 12 of 16 = 75% = C — **and all 2s was 50% and all 1s was 25%, so the bottom TWO levels of a four-level rubric both collapsed into an F.** §3.6 names the arithmetic word for word. Now **1→60, 2→73, 3→87, 4→100**, Lamar's ladder, averaged per mark: **A · B+ · C · D-.** **⭐ AND THE CHECK THAT SHOULD HAVE CAUGHT IT NOW EXISTS** — `check-writing` tested both ends for seventeen versions and **never the middle**, which is how a defect survives with a check pointed at it. §3.6 names the test in four words: *"check the middle of the range."* Derived from `RUBRIC_BANDS`, never typed. **Six negative tests, all six red.** **⚠️ One assertion INVERTED, not deleted — bottom-marks is now a D-, and that one needs Gigi.** **Nothing was re-graded because nothing had been graded** — `writingMarks` was empty. **⭐ AND `npm run check` WAS RUNNING 17 OF THE 26 CHECKS.** Nine missing outright — `check-curriculum-volume`, `check-delivery`, `check-garden`, `check-khan-units`, `check-projects`, `check-sciencelab`, `check-standards`, `check-videos`, `check-writing`. **`START-PETAL-PESTLE.bat` calls `npm run check` on first-time setup, so on Azianna's laptop a third of this app's checks had never run at all.** Cause: seventeen script names typed by hand into `package.json`. **`scripts/run-all-checks.mjs` now reads the folder** — add a check tomorrow and it runs tomorrow, no edit anywhere — in readable batches of six, naming every failure, and **refusing to run if it discovers fewer than twenty scripts**, because *"0 of 0 passed"* is the most dangerous line a build script can print. **All 26 pass in 79s, including the nine that had never been in the command — nothing was hiding in them.** Four negative tests on the runner, all four catching. **⭐ AND THE BACKUP CAN NO LONGER FORGET A TABLE — the first item of §32.1, done.** `EXPORT_TABLE_POLICY` sits beneath `db.version(7).stores({})` in `db.js`, and `check-import` now asserts **schema → policy → export → import**, both directions at every link, **finding the highest `db.version` rather than assuming 7.** **And the parent passcode is tested for the first time in twelve versions.** **Eleven negative tests, all eleven red — and three of them found something, all three in the check:** a `[^)]*` scan that stopped at the arrow argument in `meta.filter((m) => …)`; an `.includes()` satisfied by `data.writingMarksXX`; and **a note that printed "the passcode is stripped" while the assertion beside it was failing.** *A note that says more than its assertion tested is the same class of lie as a check that overstates.* **⭐ AND §32 NO LONGER CLAIMS AN ORDER IT HAD NOT READ.** `claude/BLUEPRINT_A_LOCAL_FIRST.md` — all 68 pages, extracted from Gigi's PDF, with a header saying the PDF is authoritative and which tables the extraction flattened. **The root cause of the whole expensive lesson was that the standard existed only as an upload.** Then §32 was re-derived from the real §7.6, page 68 — and **v3.55's §32 was wrong in four places while claiming to be "taken from the blueprint's own §7.6 batching, not invented here."** **Goals sat in Batch D; the standard puts them in Batch A. Rubrics were promoted to "Now" without saying so. Onboarding §4.9 was missing entirely. And accessibility was filed as Batch D work when §4 is a LAUNCH GATE (§5.6) and appears in no batch at all.** The spine was right; the provenance claim was not checked. **Every departure now carries its reason and a way back (§32.0, seven of them, D1–D7).** **35 quoted fragments in the new §32 were verified verbatim against the file on disk; five were corrected first** — one had dropped the word *diagnostic* from a QA-matrix row, three elided bracketed text without an ellipsis. **Documents only. No code changed, and `buildStamp` was deliberately NOT bumped** — bumping the number on screen when nothing on screen is different destroys the one thing `buildStamp` is for. **That is a real conflict with rule 16 and it is Gigi's to settle, not mine.** |
| **3.55** | Aug 18 | **⭐ A STANDARD NOW SITS ABOVE THIS APP.** Gigi supplied `BLUEPRINT_A_LOCAL_FIRST.md` v1.1, 68 pages, and asked the obvious question: *"I wanted a standard based on Lamar's DOC and it keeps going further and further away from it."* **She was right.** Full audit at `claude/blueprint-conformance-audit.md`: **7 of 28 anti-patterns conformant, 5 partial, 16 not; 11 of 34 architecture requirements conformant, 6 partial, 17 not.** **THREE DECISIONS TAKEN THAT MORNING WERE OVERRULED BY THE STANDARD AND WITHDRAWN** — the rubric mapping (a named failure mode, not a preference), the single binary pass bar (*"never use a single binary cutoff"*), and grading on an end-of-lesson test (*"measures recognition, not retention"*). **THE DIAGNOSTIC RE-TAKE CAME OUT OF "SETTLED AND NOT TO BE REOPENED"** and got a way back that is an EVENT, not a date. **GEOMETRY AND MEASUREMENT NOW READ "INCONCLUSIVE", NOT "2.00"** — they were never measurements. Four negative tests, all four catching. |
| **3.54** | Aug 18 | **THE THIRTEEN FLAT HERBALISM LESSONS WERE ALSO HALF A BANK.** hb-1-01 to hb-1-13 held **five** questions each while the other 243 held ten — for twenty-one versions, with both documents calling them "flat cards" and both meaning the PROSE. **65 questions written; all 256 lessons now own ten; the bank reached 2,560.** `LESSON_TEST` (6 practice + 4 scored) and a **derived** `LESSON_BANK_MINIMUM` went into the engine, and `check-assessment` fails any lesson too thin for the split. Herbalism's answer spread improved 235/239/238/**183** → 248/252/251/**209** as a side effect. |

| v | Date | What changed |
|---|---|---|
| **3.53** | Aug 18 | ⭐⭐ **THE HUMAN BODY IS FINISHED — 64 OF 64 — AND WITH IT PETAL & PESTLE ACADEMY HAS WRITTEN EVERY LESSON IT OWES.** **256 of 256 lessons · 2,495 questions · 256 verified videos**, every one checked at `youtube.com/oembed` with its length read off a duration badge. **M13 EYES AND EARS** — the oldest self-experiment in vision, her own **blind spot** found with a card and two marks and measured in centimetres, once per eye; rice dancing on a stretched drum skin; the three smallest bones she owns; and the discovery that **balance is an ear job**. **⚠️ M14 GROWING IS THE MODULE A BODY COURSE DOES DAMAGE WITH, AND ITS FILE HEADER IS A SET OF RULES, NOT NOTES.** **HEIGHT ONLY. NO WEIGHT ANYWHERE** — not in a prompt, not in a choice, not as a unit being practised. **She is compared with HERSELF** and never with another child, never with her age, never with a chart of what is usual. **Three bank questions make "there is no right height" or "compare with your own last measurement" the correct answer, on purpose.** **Nothing about puberty** — that is Gigi's conversation to have in her own words, not a paragraph a machine wrote into a Tuesday. Her height is measured **once, dated, and not again that week**; the repeatable measurement is a **fingernail**, marked and read in millimetres every third day for a fortnight and plotted as her **first line graph** — a body part nobody has ever been made to feel bad about. **M15 STAYING WELL** — a genuine controlled experiment on her own two hands with oil, pepper, soap and a timer, twenty seconds each, where **the fairness is the lesson rather than the soap**; then every defence she has traced back to a module she already did — skin from M2, stomach juice from M9, white blood cells from M6. **Fever taught as something the body does ON PURPOSE**, and **nobody's temperature is taken, including her own** — one question makes "nobody's" the right answer. **⭐ M16 BLACK WOMEN IN MEDICINE, AND IT IS LAST BECAUSE IT IS THE PAY-OFF, NOT THE PREFACE.** Dr. Rebecca Lee Crumpler (1864) · Dr. Patricia Bath (1986) · Dr. Alexa Canady (1981) · Dr. Mae Jemison (1992). **She meets Dr. Bath AFTER finding her own blind spot, and Dr. Canady AFTER a two-point test on her own skin** — so when she is told a Black woman invented the laser that clears a cloudy lens, **she already knows what a lens is**. Each lesson leads with **what she did**; the barrier is stated once, as fact, and **never as the point of the story**. Nobody here is remarkable for having endured something. **And Module 16 carries the biggest arithmetic of the year** — a timeline at **one centimetre to one year** from 1860 to 1992, built across all four lessons on one strip, with **four-digit subtraction checked twice, once on the ruler and once on paper, and they have to agree**. The last thing the course asks her for is not an answer: **it is a question she still has.** **⚠️ ONE VIDEO REJECTED, AND IT IS THE THIRD OPERATION OUCH ID TO FAIL THE SAME WAY.** `cuwosIye2pQ` verified perfectly at oEmbed and then **dropped out of a playlist** — the region-lock pattern from v3.48. **A video that verifies and will not play is worse than one that never verified**, because it passes every check and fails only in front of the child. **⚠️ THE BLACK-EDUCATOR CHANNEL GAP IS STILL OPEN AND MODULE 16 DOES NOT CLOSE IT.** The **subjects** of those four lessons are Black American women. The **channels** are **not claimed to be Black-led**, because that has not been verified, and each `sourceGap` says so in those words. Across sixteen modules no Black-educator-led children's anatomy channel was found. **Unknown is recorded as unknown.** **⚠️ AND A ONE-CHARACTER MISTAKE OF MINE WENT RED IN THREE CHECKS AT ONCE.** Updating the Human Body count I ran a find-and-replace on `48` and hit **The Science Lab and Social Studies as well**, claiming 64 written lessons for each against 48 on disk. **`check-curriculum-volume`, `check-delivery` and `check-sciencelab` all named it, by course, within seconds** — the first time all three of those drift assertions have fired together. **⭐ AND THE BODY-IMAGE RULE IS A CHECK NOW, NOT A HABIT.** Gigi's standing rule for this course — **no weight, no body composition, no appearance** — had been enforced for sixteen modules by me reading what I wrote, **which is not enforcement**. A **hand-run** scan at the end of Quarter 4 found the word *weight* inside **Module 14, the growing module**, in a wrong-answer feedback line. Harmless in isolation, and still the word arriving on the screen of a nine-year-old girl in the one lesson about her own body changing. **A rule she must act on is a CHECK, not a warning**, so it is one: weight and appearance language attached to a **person** is banned across all 640 Human Body questions, and that vocabulary is banned **outright** in Module 14 where there is no innocent reading. **⚠️ And my first draft of that check failed twenty CORRECT items** — it matched `too big` and `too small`, which in a course built on measuring things are maths feedback about a **number** (*"Ten times too small"*, *"Undissolved bits are too big for the holes"*). **A check that fails correct content teaches whoever hits it to reach for the check instead of the content**, which this file has now done four times. Narrowed to weight words with a **person** as the subject; *big* and *small* are out of the pattern entirely. **Four negative tests, all four catching — and the fourth is INVERTED:** *"ten times too small, and far too big"* must NOT fail, and correctly does not. **The Human Body is now marked `complete`, and that was enforced rather than remembered:** v3.31's rule is that a course which has written every lesson it owes **may not say `building`**, and the build stayed red until the word changed. |
| **3.52** | Aug 18 | **⚠️ SIMULATE-YEAR WENT RED THE MOMENT MODULE 12 LANDED, AND IT WAS RIGHT ABOUT HALF OF IT.** Across 260 simulated school days the average question had been retrieved **2.0 times**, and the check says plainly: *once is not review*. **THE REAL BUG WAS IN `pickWarmUp`.** On a morning with fewer than three questions due it filled every spare slot with questions she had **never met** — so she met three strangers and no old friends, and the fault got **worse every time a module was written**, because every module adds forty more strangers to choose from. **The order is now: due items first, then AT MOST ONE new one, then questions she has already met pulled forward — fewest retrievals first.** A question met once in October and never seen again is the one at risk, and it is now the one that comes back. **⚠️ AND THE CAP IS A PREFERENCE, NOT A CEILING — `check-assessment` CAUGHT THAT INSIDE THE HOUR.** The first version made it a hard ceiling, so on a morning when nothing is due and nothing is known — **every child's first week** — the warm-up returned **one question instead of three**. Strangers top it up when there is genuinely nothing else to ask, which on day one is the only honest thing it could do. **⚠️ THE OTHER HALF WAS THE CHECK CLAIMING MORE THAN IT TESTS.** The old assertion averaged over **every question she ever met**. Most enter the boxes through a **weekly test**, not the warm-up, and a weekly test introduces about seven at a time — so the number fell every time a module was **WRITTEN**. **A check that goes red because the curriculum grew is a check that pressures you to stop writing curriculum**, which is §4 word for word. The claim is now made over **warm-up slots**, which is what the warm-up governs; the whole-bank figure is **printed as a number rather than counted as a pass**. **⚠️ AND ONE ASSERTION IS REPORTED AS UNRUN RATHER THAN GREEN.** The re-meeting ratio was negative-tested by raising the new-question cap from 1 to 3 — **the exact mutation it exists to catch — AND IT STAYED GREEN**, because the overdue backlog fills all three slots every morning and the branch never fires. **5 first meetings in 777 slots cannot tell a working cap from a broken one.** It prints as **UNRUN, not passed**. The assertion that did bite under mutation is the per-day one, and it caught the old code on **day 2**. A first draft that averaged over the four questions the warm-up introduced is **written into the file as REJECTED** rather than quietly dropped — an assertion over four items is a check-shaped decoration. **⚠️ AND THE BACKLOG IS NOW ON THE SCREEN, BECAUSE IT IS GIGI'S CALL AND NOT MINE.** Three questions a morning cannot cycle a **2,335-question bank**. Mean gap between meetings **63.8 days**, longest **166**. Nothing is broken — nothing goes overdue and nags, which was the whole design — but **"three a morning" was set when the app held one course and it now holds four.** §7: *a contradiction in what she asked for is a question, not a judgement call.* **Two negative tests, both catching after the second was rewritten.** |
| **3.51** | Aug 18 | ⭐ **QUARTER 3 OF THE HUMAN BODY IS COMPLETE — 48 OF 64, THREE QUARTERS OF THE COURSE.** Modules 9 to 12: *The Mouth and the Stomach* · *The Gut, a Long Tube* · *The Kidneys and Water* · *The Brain and the Nerves*. **240 lessons and 2,335 questions app-wide, and every one of the 240 carries a video verified at `youtube.com/oembed` with its length read off a duration badge.** **⭐ THE WHOLE QUARTER IS BUILT ROUND THE FOUR ITEMS THAT HIT HER TEST FLOOR.** Elapsed time (**0 of 1**) times a cracker until it turns sweet, and times two hands drying. Units (**0 of 3**) converts metres to centimetres on **seven metres of real string laid out and walked beside**, millilitres to litres on **1,200 ml held in both hands**, and millimetres to centimetres on a bedside test done on her own skin. Area (**0 of 2**) and perimeter (**0 of 3**) are counted on squared paper to work out **why a folded gut lining catches more than a smooth one**. None of it is arithmetic drill — **every number is the answer to a body question.** **M9** — three shapes of tooth for three jobs, most of a tooth buried in the jaw, and the **plain cracker chewed and TIMED** until saliva turns its starch to sugar. **M10** — the body as a doughnut, so **food in the tube has not entered you yet**; muscle waves that work upside down; and a lining folded like a towel to buy surface. **M11** — two fist-sized filters found by pressing on her own back, a **real filter built from a bottle** with the water measured in and out, and the discovery that a wet hand only cools you **once it starts to dry**. **M12** — the **two-point test in millimetres** on four patches of skin, a genuine neurology bedside test and completely safe; reflexes that skip the brain entirely; and the reason a doctor always asks **exactly where it hurts**. **⚠️ THE SAFETY FENCE TIGHTENED THREE TIMES THIS QUARTER, IN WRITING.** Food: nothing about calories, amounts, good and bad foods, or weight. **Kidneys: NOTHING about how much anybody should drink and NO self-inspection of any kind — no urine colour, no checking yourself for signs**, which is the one thing this course must never teach. Brain: **nothing about how clever anybody is, how fast anybody learns, or one brain being better than another** — this is a child who has just been told she is behind, and the brain module is where that would land hardest. Two questions state outright that **an imperfect result means nothing is wrong**. **⚠️ TWO VIDEOS REJECTED AND BOTH WRITTEN DOWN.** `Pd7NYJKYT7Q` (Operation Ouch, kidneys) returned an **empty oEmbed body**, which is what an unavailable id does. `kj-L-6MfaE0` (Operation Ouch, reaction time) **verified at oEmbed and then dropped out of a playlist twice** — the same pattern the region-locked BBC Teach video showed at v3.48. **A video that verifies and will not play is worse than one that never verified.** `GUCcsMmZVec` rejected on length alone: **44:56 does not fit a 30-minute block.** **⚠️ THE BLACK-EDUCATOR SEARCH IS STILL OPEN AND STILL RECORDED AS UNKNOWN.** A new search this round — for Black science educators making children's anatomy videos — returned round-up listicles naming Homeschool Pop, Colossal Cranium, Operation Ouch and Kids Learning Tube, plus one promising name, **"Learning Science with Michelle"**. A follow-up search for that channel on nervous-system topics **returned nothing**. Every failed search is written into the `sourceGap` of the lesson it was run for. **Unknown is recorded as unknown, never as a gap closed.** |
| **3.50** | Aug 18 | ⭐ **QUARTER 2 OF THE HUMAN BODY IS COMPLETE — 32 OF 64, HALF THE COURSE.** *The Lungs* — two lungs of different sizes, a branching tree of air pipes, and the discovery that **lungs have no muscle at all**: the diaphragm pulls down, the chest enlarges, and air falls in. **Breathing is done TO the lungs, not BY them.** Then *Listening to a Chest*, the instrument the whole quarter was building toward. **Module 7 carries two measurements** — breaths a minute, and **one breath measured in MILLILITRES** with a marked bottle upturned in a bowl. Millilitres are a unit she had not met, and units scored **0 of 3** on her Check-In. **And it teaches a real piece of method:** counting your own breathing CHANGES it, because breathing is the one thing both automatic and controllable. That is why a doctor counts breaths while appearing to take a pulse. She tests it on a grown-up twice — once secretly, once announced — and **the difference between the two numbers is the finding**. **⭐ MODULE 8 IS BUILT ROUND ONE PERSON, ON PURPOSE.** Three of its four videos are **Dr. Yama, a physician who makes medical videos for children**. Azianna intends to be a doctor. Every other module borrows a science channel; **this one borrows a doctor**, using a real instrument, explaining what she is hearing. **THE TIGHTEST SAFETY RULE IN THE COURSE SITS HERE.** A stethoscope is a **listening** tool, not a diagnostic one, and Module 8 says so in four places. She listens, she describes, and **she never decides whether a sound is normal** — that is not a question a nine-year-old should answer about anybody's chest. Anything unexpected means tell a grown-up. **The last lesson of the quarter repeats the first lesson of the course** — observe, ask, measure, with the same grown-up, except **measure is now three things instead of one**. Her ledger question is what she can do now that she could not do in week one. |
| **3.49** | Aug 18 | **QUARTER 2 IS HALF BUILT — MODULES 5 AND 6.** *The Heart, a Pump* — four rooms, one-way valves, the two trips blood makes, a hundred thousand beats a day, and **resting-versus-working pulse read as ONE finding** rather than two numbers. *Blood, and What It Carries* — plasma and three parts with three jobs, arteries and veins, **80,000 km of vessel**, and what the cuff on an arm actually measures. **24 of 64.** **The reading cap rises to 12 words a sentence** — Quarter 2 on §10.1's ramp, one word more than Q1, because her Grammar & Usage measured 2.15. **⭐ THE ANSWER SPREAD IS BUILT IN NOW RATHER THAN CORRECTED AFTERWARDS.** Modules 1 and 2 were hand-written and **both needed rebalancing after the fact** — M2 landed at 44%, over the bar. These two banks were **generated with each question's correct slot assigned from a fixed balanced pattern as the file was written**, and both came out **10/10/10/10 on the first run**. A property of the build instead of a defect found later. **BLOOD IS THE TOPIC MOST LIKELY TO DRIFT**, so Module 6 stays narrow on purpose: no blood types, no donation, no illness, and **no number she is invited to call high or low**. In the cuff activity **she watches** — she does not put a cuff on anybody, including herself — and *who reads a blood pressure* answers **a real doctor**, every time. **⚠️ Two more long videos, recorded rather than quietly dropped:** the Operation Ouch cardiovascular compilation is **37 minutes**, the longest in the app, and the blood one is **29**. Both are compilations that stop and restart cleanly. Gigi's call stands — the full videos stay — and neither fits a single 30-minute block. |
| **3.48** | Aug 18 | ⭐ **QUARTER 1 OF THE HUMAN BODY IS COMPLETE — 16 LESSONS, 160 QUESTIONS, EIGHT REGISTERED WEEKS, AND THE QUARTER EXAM CAN OPEN.** M1 *What a Doctor Does First* · M2 *Skin, the Cover* · M3 *Bones, the Frame* · M4 *Muscles, the Pull*. **Gigi: *"Azianna is currently working on them on her school days. I do not want her to fall further behind. School started aug 3rd and its aug 17th."*** She is in **week 3** — Module 2 — so there are now **six weeks of lessons ahead of her instead of one.** **MODULE 3 IS AIMED SQUARELY AT HER TWO FLOORS.** Geometry 2.00 and Measurement 2.00 both bottomed out — units 0/3, perimeter 0/3, area 0/2. She measures **height and arm span in centimetres AND inches**, then traces **her own hand on squared paper**: area by counting squares inside, perimeter by measuring round the outline. **In the ACTIVITY — the prose never mentions measuring.** **MODULE 4 RUNS ON ONE FACT: A MUSCLE CAN ONLY PULL.** Which is why they come in pairs, why an elbow needs two, and why a reflex test works. Its measurement is **reaction time with a dropped ruler** — elapsed time, her weakest single item at 0 of 1. **⚠️ THE SAFETY SCAN FIRED ON MY OWN NEW QUESTION.** A *why* line read *"It is a clamp, not a cure"* and the dosing scan caught **cure**. Reworded to *"It holds the ends still. Your body does the mending"* — which is the better sentence anyway. **v3.10's rule, fired for real: reword the content, never loosen the check.** **⚠️ AND A VIDEO WAS REJECTED FOR A NEW REASON.** `3haTJCOkyxA` — *How do our muscles and bones work? | BBC Teach* — **verified perfectly at oEmbed and then failed to load in a playlist twice**, almost certainly UK-only. **A VIDEO THAT VERIFIES AND WILL NOT PLAY IS WORSE THAN ONE THAT NEVER VERIFIED**, because it passes every check and fails only in front of the child. |
| **3.47** | Aug 18 | **MODULE 2 — SKIN, THE COVER.** The largest organ she owns · sweat and goose bumps as one thermostat working both ways · reading the four stages of a healed cut · and why fingers wrinkle, **which is really a lesson about a wrong idea nobody tested for years.** **Nothing in it is about how skin LOOKS** — not colour, not marks — and the healing lesson uses a cut that has **already healed**. **8 of 64.** **⚠️ AND THE ANSWER-SPREAD RULE TURNED OUT TO GUARD ONE COURSE ONLY.** Written at v3.24 after a draft put 42 of 60 answers in slot B and none in D — and it lives in `check-sciencelab.mjs`, which imports `SCIENCELAB_COURSE_BANK` and nothing else. **Herbalism's 895 questions, Social Studies' 480 and The Human Body's 80 had never been measured by it.** *A file named after one course never stands in for the whole app* — v3.25's rule, applied to `check-standards` at v3.27, **never carried across to this one.** **⚠️ IT FOUND FOUR BROKEN HERBALISM MODULES.** `herbalism-m7`: **44 of 60 in slot B — 73%, worse than the original bug that created the rule.** `herbalism-m15`, the **Black Women in Medicine and Botany** module: 39 of 60 in B and **not one answer in slot D across sixty questions**. `m16` the same. `m6` at 48% in A. **A child could have scored 73% on Module 7 by pressing B every time.** **FIXED WITHOUT CHANGING A WORD** — choices and their feedback rotated together, so every prompt, every *why*, **every correct-answer text** and every wrong-choice explanation is byte-identical; only the slot moved. **Verified question by question against a backup: 230 questions, zero content differences.** Herbalism's worst module went from **73% to 26%**. **My own Module 2 bank is what exposed it** — it landed at 44% and all 26 checks went green, and the number was only seen because I printed it by hand while wiring the course up. **Four negative tests, all four catching.** |
| **3.46** | Aug 17 | **THE HUMAN BODY HAS ITS FIRST LESSONS — MODULE 1, *WHAT A DOCTOR DOES FIRST*.** Four lessons, 40 questions, two registered weeks, **wired into the app-wide door on the day it was written** rather than after — the v3.25 failure, refused in advance. Her Tue/Thu 2:45 block opens it. **The measurement is in the ACTIVITY, never the prose:** 15 seconds × 4, which also feeds her Khan *Intro to multiplication*, against a Measurement strand that hit the floor at 0 of 3 on units and 0 of 1 on elapsed time. **And nothing in it teaches her to diagnose anybody** — every activity carries its own safety line, no weight, no body composition, and the one question that asks about differing numbers answers *people differ*. **⚠️ THE LESSON IDS COLLIDED WITH HERBALISM AND THE CHECKS CAUGHT IT.** `hb-body-m1-01` starts with `hb-`, which is how `check-delivery` identifies a **Herbalism** question — so 40 Human Body questions were counted as Herbalism's and its bank read **935 instead of 895**. The question ids collided outright: `t-hbm101a` already belonged to Herbalism. **Gigi's condition when the door was widened — that Herbalism must not move — is the assertion that caught it.** **⚠️ `check-curriculum-volume` HELD `humanbody: 0` AS A LITERAL, FOR THE THIRD TIME IN THE SAME FILE.** At v3.34 it held `social: 0`, and the note then was exact — *a number that happens to be correct today is not a measurement*. **The fix went to the instance, not the shape**, and a fresh zero was typed on the next line. Derived from the app-wide door now. **⚠️ AND v3.42 MADE A PROMISE IT DID NOT KEEP.** Its derived exemption said *"it will fail itself the day The Human Body gets its first week."* It did not — the block still read *still being written*, because `blockLinks` returned that notice unconditionally and `check-links` only asked whether a target **existed**, and **a notice is a target**. **A PROMISE A CHECK MAKES IN A COMMENT IS NOT A PROMISE THE CHECK KEEPS.** **⚠️ A NEW ASSERTION WOULD HAVE CRASHED RATHER THAN REPORTED** — written above `const errors = []`, so `errors.push` throws *Cannot access before initialization*. It passed every run only because the counts agreed and **the branch never ran**. Found by forcing it to fire. **⚠️ And a comment in my own new bank stated a figure nothing had counted** — *"A 10 · B 10 · C 10 · D 10"* against a real spread of **A 9 · B 12 · C 15 · D 4**. Passes at 38% with every slot used; the comment was the intention written down as the result. **VIDEO LENGTH IS A GUIDE, NOT A RULE — Gigi:** *"Just leave the videos alone the way they are. It maybe good to have her watch the full videos."* The ceiling was my inference from §10.2, **not her rule**, and not a law about how she spends forty-five minutes with her granddaughter. Measured on all 192 and printed every run; the hard failure and its thirteen declarations are gone. **Six negative tests, all six catching.** |
| **3.45** | Aug 17 | **A VIDEO MUST FIT THE LESSON IT SITS IN — AND NOTHING HAD EVER CHECKED.** `video.minutes` has been recorded on every lesson since v3.24 and **not one of the twenty-six checks read it.** **A field recorded and never asserted is a field that drifts** — Social Studies reached **twelve videos over six minutes, one of them eleven**, in a course whose System Concept is ten. **THE CEILING IS DERIVED:** §10.2 gives the System Concept 12 minutes at 45 and 10 at 30, and that block holds **the video AND two beats** — so `ceiling = systemConcept − 4`, giving 8 and 6. Typing 6 and 8 is correct today and wrong the day a course's minutes change. **FOUND BY MEASURING THE DISK RATHER THAN REASONING:** a 9:19 Human Body video looked fine by argument until the finished courses were measured — **The Science Lab has the identical 30-minute shape and its 48 videos run 3 to 5 minutes.** Four of the first nine Human Body picks were rejected on length and **one on LEVEL** — a knee-jerk video from an adult neuroscience channel, the v3.40 failure exactly. **THE THIRTEEN ALREADY OVER IT ARE DECLARED BY GIGI**, by id, dated, with a reason — *the check is new, the content is not*. A declaration names ids and **does not raise the ceiling**; a stale one fails the build as a stale `DECLARED_OMISSION` has since v3.33. **⚠️ One of the thirteen is Frederick Douglass by Seed of Melanin Kids** — one of only four videos in the app answering the Black-American-educator requirement. **Cutting it for length would have traded the scarcest thing here for the newest rule.** **⚠️ AND IT IMMEDIATELY FOUND SOMETHING BIGGER: 85 OF HERBALISM'S 96 VIDEOS HAVE NO RECORDED LENGTH — 44% of the app.** The field arrived with The Science Lab at v3.24; Herbalism was written before it existed and nothing went back. **THOSE 85 DO NOT FAIL THE BUILD, DELIBERATELY** — hard-failing them leaves one fast route to green, which is to **invent 85 durations nobody measured**, and *a check that pressures you to falsify the data is worse than no check* (v3.24, fired at v3.40). **The gap is printed in full every run**, and the hard failure is forward-looking: **a video verified on or after 2026-08-17 must carry its length.** No list, and it expires on its own. **Seven negative tests. Three could not run at first** — mutations that missed a trailing comma and an inline field — **recorded as UNRUN, never green**, then rewritten until they bit. **One is INVERTED:** an old video losing its length must NOT fail, and correctly did not, landing in the printed gap. |
| **3.44** | Aug 17 | **GIGI CAN SAY SOMETHING BACK TO A JOURNAL ENTRY — AND IT IS NOT A GRADE.** *"Her journal is entered but there isn't a way to grade it or give feedback."* Grown-Up Corner → her Journal → **"Say something back"**, which sends a note to **"Notes from Gigi and Mom"**, from Gigi or from Mom. **FEEDBACK IS NOT GRADING** — Gigi's own distinction in the backlog. **No score, no rubric, no percentage, and no way to edit a word she wrote.** **It reuses the existing notes channel** rather than growing a second one: a near-identical path would have meant a new table, a database version, an export change and an import change — four places to get wrong to do a thing already built. **⚠️ ON THE AI QUESTION.** Gigi asked whether the Journal could be graded by AI and chose to see the offline version first. **There is no model and no network call anywhere in `src/`** — the only `fetch` reads a local export off her own disk. Adding one needs her own API key and a live internet connection, **breaking the offline design the bell was synthesised for**. If a model is ever added it may only write a **draft into this same box for her to edit and send**: a grown-up stays between the machine and the child, and the entry itself is still never scored. **⚠️ AND THE NEW CHECK FAILED A CORRECT SCREEN ON ITS FIRST RUN.** It located `JournalNote` by name then matched braces from the first `{` — which for `function JournalNote({ entry })` is the **destructured parameter list**. The "body" it read was twenty-nine characters. **A check that reads the wrong region is v3.17; one that reads the wrong function is v3.41; this was both.** The parameter list is skipped explicitly now and **an implausibly small region is itself an error rather than a silent pass**. Four negative tests, all four catching. |
| **3.43** | Aug 17 | **THE BELL IS LOUDER AND STAYS ON ALL DAY · READ-ALOUD STOPPED SAYING EVERY LESSON TWICE · THE GARDEN HAND-OVER FINALLY HAS A TEST.** Bell volume **0.22 → 0.62**, and the character changed, which carries further than the number: two sine partials was a **chime**; it has **eleven inharmonic partials** now, a **2ms hammer** instead of a 6ms swell, three strikes. **⚠️ AND THE BELL WAS NEVER TURNING OFF — IT WAS NEVER BEING REMEMBERED.** `bellOn` was `useState(false)` in TodayView, so it reset when she left the tab and on every reload; **the screen said so out loud** — *"for as long as this tab stays open"* — an honest description of a bug nobody had recognised. It is on her saved day row now and ends at the **end of the school day, READ OFF the last block** rather than typed. **After a reload it wakes on her first tap** — the browser's rule is not defeated, her next real gesture resumes the audio; without it the flag would say ON while the room stayed silent. **⚠️ NOTHING IN TWENTY-SIX CHECKS HAD EVER MENTIONED THE BELL.** **⚠️ READ-ALOUD READ EVERY LESSON TWICE:** `lessonChunks` read `core` then `beats` unconditionally and **179 of 192 lessons carry both fields with the same content** — `hb-m1-01`'s core headings and beat labels are the same two strings — so it said the whole lesson, *"Go and do this."*, then the whole lesson again. **Every check asks about the SCREEN; the read-aloud is a second rendering through a different function and nothing had ever compared the two.** **⚠️ AND THE GARDEN & PROJECTS CAVEAT WAS SOFTER THAN THE TRUTH** — *"tested against synthetic weeks"* was true once, by hand, at v3.31: **`hasRunOut` and `courseForBlockOnDay` were called by none of the 26 scripts.** Driven on the **real week table** now, three states each, and **still not claimed to have been seen on her own progress.** **Thirteen negative tests; four missed and every miss found something** — one assertion satisfied by an **import name**, one by an **`if` guard** rather than the line that speaks, and **two rules reachable only through a function that short-circuits before them.** **Two mutations could not fail at all** — one because my literal happened to be today's correct number, one because two guards overlapped — **both recorded UNRUN, never green, and both made testable.** **⚠️ And my own new check failed a correct screen on its first write**, matching `useState(false)` next to a comment containing the word "bell": **a check that locates the thing it guards by a comment is guarding the comment**, v3.38's rule, written into a check by the person who wrote that rule down. |
| **3.42** | Aug 17 | **⚠️ "IN THE TODAY SCHEDULE, THERE AREN'T LINKS TO SCIENCE AND SOCIAL STUDIES." GIGI, AND THE LINK HAD BEEN SWITCHED OFF ON PURPOSE TWELVE VERSIONS AGO.** `blockLinks.js` held `if (subject === 'science') return null;` and the same for social, under a comment that **dated itself**: *"The Science Lab's lessons are still being written."* **The Science Lab shipped all 48 at v3.30; Social Studies shipped all 48 at v3.37.** TodayView renders its button inside `{target && …}`, so null rendered **nothing at all** — the block named a subject and made her go find it, **the v3.20 complaint exactly**. **⚠️ AND `check-links` WAS GREEN THROUGHOUT**, because both subjects sat on a **hand-written `DELIBERATELY_UNLINKED` list** whose reasons were true and were **about Khan** — and Khan was never the question. **THE EXEMPTION IS DERIVED NOW: a subject may be unlinked only if the app has written zero lessons for it.** It would have failed itself the day The Science Lab shipped and **will fail itself the day The Human Body gets its first week.** **⚠️ A SECOND FAULT UNDERNEATH: THE LABEL AND THE LINK READ DIFFERENT FACTS** — the 2:45 block's name is built from the day and her progress and correctly said "The Human Body" on a Tuesday, while the target function only ever saw the string `'social'`. Both read the same inputs now and the check **compares label against link on all five days**. **⚠️ AND THE LINK WOULD HAVE OPENED THE WRONG COURSE** — `LessonsView` starts on `APP_COURSES[0]`, so "Social Studies" would have opened **Herbalism**; every link carries its course now, and **Herbalism's was accidentally right for that reason all along**. **⚠️ A NEGATIVE TEST MISSED AND FOUND A REAL HOLE:** inverting the run-out rule so the block hands its time to the garden with all 48 lessons unread **passed everything**, because the garden is a well-formed target — **the check asked "is there an answer", never "is the answer right", which is v3.21 word for word.** **And one negative test could not run at all** — a mutation whose indentation did not match, so it changed nothing; **recorded as UNRUN, never as green**, and re-run properly. **Eight negative tests, all eight catching.** |
| **3.41** | Aug 17 | **THE VIDEO MOVED TO THE TOP OF ALL 192 LESSONS — AND IT WAS ONE EDIT, NOT 192.** The video renders from the lesson's own `video` field in a single component, and **no lesson file embeds a video in its prose** — asserted across all 40 lesson files, zero hits. **⚠️ AND IT WAS NOT WHERE ANYONE THOUGHT.** Gigi's brief said it sat inside System Concept beat 1; the comment in `LessonReader.jsx` said the same; §10.2 of the master plan said the same. **All three were wrong.** It rendered *after* Part 1, Part 2 and both Apply-Its — **under every word of prose in the lesson**, so a child whose Reading 3.46 is a **listening** score read the entire lesson before reaching the thing meant to explain it. **The wrong comment is where the wrong plan came from**, which is v3.38's rule arriving from the other side: that time a check located what it guarded by a comment, this time a document did. **New order: header · read-aloud · hook · the question · ▶ VIDEO · Part 1.** **`check-delivery` assertion 7** holds it by the **position of rendered elements with all comments stripped**, never by looking for the word "video" — the v3.40 mistake where a check found "rubric" inside *"Read the rubric first."* **⚠️ AND MY FIRST DRAFT OF THAT ASSERTION WAS DEAD ON ARRIVAL:** it located the hook question by searching the whole file, which resolved to `lessonChunks()` at line 58 — the read-aloud helper, **above every JSX element** — so the "video must not float above the hook" branch could never fire. **Its negative test caught it: floating the video to the top of the page passed silently.** Positions are now measured only inside the component that renders. **Six negative tests, all six catching** — video below the prose, video back inside Part 1, video above the hook, the block deleted with a comment left claiming it, a lying comment that cannot rescue a broken layout, and a stale comment that cannot break a correct one. **Two counts corrected:** the checks were never "26 plus four", and **the v3.38 note declaring `petal-pestle-v3.31-social-studies-shape.md` "never written" was itself false — the document is 137 lines and always existed.** **A document declared missing while it sits on the shelf is the same fault as one referenced and absent.** **AND THE `claude/` FOLDER DID NOT EXIST ON DISK AT ALL** — every document the plan cites by path lived only in project knowledge, read-only. All 36 are now on disk, verified identical by sha256. |
| **3.40** | Aug 17 | **⚠️ GIGI CAUGHT THE WRITING PROGRAMME TEACHING ONE THING AND ASKING ANOTHER, AND SHE WAS RIGHT THREE TIMES OVER.** *"the journal was teaching about verbs that doesn't end in ed and asked her about a verb run with[out] telling her what the past tense is... All lessons needs to be at the learning level."* The lesson showed go, see and bring and asked for the past of **run, write and grow** — a quiz wearing a lesson's clothes. **FORTY OF THE SEVENTY-TWO read above a 3.0 grade level**, one at 8.0, against a child whose **Grammar & Usage measured 2.15**; nothing had ever measured them, because check-readability guards Check-In items and check-assessment guards bank questions and this prose fell in the gap. **AND DELIVERY HASHED THE DAY KEY**, so there was no order at all and sentence structure never got built. **Rebuilt: 72 lessons in four stages, sentence work FIRST and uninterrupted (Gigi's call), starting at 2nd grade and climbing to 3rd–4th.** Every lesson carries its **answer**, declares its task **closed or open**, and for a closed task every content word of the answer must appear in the lesson — **and fewer than forty closed tasks fails the build**, so it cannot dodge the rule by going all-open. Every lesson **measured per stage**, with the courses' own exemption: a long word is forgiven when the lesson teaches it by name, titles capped at seven words so the exemption cannot be widened. **Order comes from HER PROGRESS** — days she has written, counted strictly before today. **Fifteen negative tests, all fifteen catching.** |
| **3.39** | Aug 17 | **THE BOOK REPORTS AND RESEARCH PAPERS ARE ON A SCREEN.** v3.38 shipped the rubrics, the five-step research sequence and the grading arithmetic in a data file and **nothing rendered any of it** — the v3.25 failure exactly, one version old. A grown-up marks a piece row by row under **Gradebook → Writing pieces**; **her Journal ends with "Writing you hand in"**, the six pieces of the year, each opening to its four parts or its five steps and **the words for what a good one looks like**. **A rubric she only meets afterwards is a mark scheme, not a teaching tool.** **WHAT IS STORED IS THE MARKS, NEVER THE PERCENTAGE** — one integer 1–4 per rubric row, in a `writingMarks` table at **database v7**, in the backup and read back by the import. **Read-aloud is a tick box, not a secret.** **⚠️ THREE NEGATIVE TESTS MISSED ON THE FIRST RUN AND EACH FOUND SOMETHING** — an export assertion that read to the end of the file, a read-aloud assertion satisfied by a parameter name, and two mutations that hit the wrong function because the store has a second `readAloud` belonging to the Check-In. |
| **3.38** | Aug 17 | **THE WRITING AND GRAMMAR PROGRAMME, IN THE JOURNAL SHE ALREADY OPENS.** Gigi: *"She needs help with grammar and writing and I wanted her journals to assist with that."* **It is not a course** — a course is a fifth block in an afternoon with none free. **Five minutes at the top of the Journal.** **THE JOURNAL IS NEVER GRADED AND NEVER CORRECTED** — no box to type in, no score, no submit, and `check-writing` reads the screen as text every run. **The graded writing is separate and she sees the rubric first:** four book reports and **two** research papers a year on Lamar's +/− ladder. **⚠️ HALF THE MINI-LESSONS COULD NEVER HAVE COME UP** — kind and index were derived from the same number over two 36-long lists. **⚠️ AND A NEGATIVE TEST GOT THROUGH:** ripping the mini-lesson out of the Journal was not caught, because the check found the block by a comment marker and `indexOf` matches a superstring. **The check was rebuilt, not the test reworded.** **Check #26.** |
| **3.37** | Aug 17 | **SOCIAL STUDIES IS FINISHED. 48 of 48 lessons, 480 questions, 48 verified videos, twelve modules across three quarters — and all 36 Georgia units it owns are taught.** **AND WITH IT, EVERY GEORGIA FOURTH-GRADE STANDARD THIS APP OWES: 61 of 61.** ⭐ **QUARTER 3 ANSWERED THE STANDING REQUIREMENT AFTER TWO COURSES OF FAILING IT** — Douglass with **Seed of Melanin Kids!**, Tubman with **The Magic In Me TV**, the taking of the vote with **Black History Files**, Jim Crow with **The Blk History Channel**, all four recorded as **likely, not confirmed**. **SS4H4a GOT THREE LESSONS INSTEAD OF ONE.** **SS4H6c AND SS4H6d GOT TWO EACH** — a year of sharecropping worked on paper until the number goes below zero, and three rules that took the vote without ever mentioning race. **THE LAST LESSON OF FORTY-EIGHT SENDS HER TO TALK TO SOMEBODY.** **GPB teaches five of the lessons.** **TWO LESSONS LOST THEIR VIDEO TO THE ADVOCACY EXCLUSION AND WERE FOLDED RATHER THAN FAKED.** |
| **3.35–3.36** | Aug 17 | **MODULES 2 THROUGH 8.** The Revolution · building a government · the Bill of Rights and the branches · moving west · **removal, which began in this state** · a country splitting · machines, money and work. **Every prompt over the reading cap was reworded, none exempted.** **One word earned the derived exemption: `telegraph`**, because Georgia's SS4E1f names it. |
| **3.34** | Aug 17 | **SOCIAL STUDIES HAS ITS FIRST LESSONS. Module 1, THE ROAD TO REVOLUTION** — delivered in the same version it was written. **⚠️ AND IT EXPOSED THE ENGINE HANDING A NINE-YEAR-OLD THE WRONG PAPER:** `buildWeeklyTest` used the global 8-question size for every course, so a two-lesson week would have produced an **eight-question test drawn from two lessons** — the exact thing `check-curriculum-volume` forbids, one file away. **A rule enforced on the number in the config, and not on the paper the child is handed, is half a rule.** |
| **3.33** | Aug 17 | **`check-standards` READS BOTH SUBJECTS.** **AND A DROPPED ELEMENT MUST BE DECLARED BY A PERSON**, with a name, a date and a reason. **A STALE DECLARATION FAILS THE BUILD TOO.** Six negative tests, all six catching. |
| **3.32** | Aug 17 | **GEORGIA'S 4TH-GRADE SOCIAL STUDIES STANDARDS EXIST ON DISK, TRANSCRIBED FROM THE GaDOE DOCUMENT.** **Two files had claimed for nine versions that this work was done**, and `check-standards` only ever imported `GA_SCIENCE_4` — **which is why nothing ever tested the claim.** **The count is settled from the primary source and both rejected counts are written into the file with their source.** |
| **3.31** | Aug 17 | **SOCIAL STUDIES DROPS TO QUARTERS 1–3; THE HUMAN BODY KEEPS ALL FOUR.** **Two quarters could not hold it honestly** — 32 lessons against roughly 37 Georgia elements, and **fewer lessons than elements is a survey, not a course.** **THE TIMETABLE HAS NO CALENDAR AND MUST NOT GROW ONE** — so the rotating block reads **her own progress** instead. **AND UNWRITTEN IS NOT FINISHED.** **HERBALISM SAID `state: 'building'` WITH ALL 96 LESSONS WRITTEN, AND HAD SINCE v3.9** — one word that switched off `check-standards`' only hard failure for twenty-one versions while printing `10/10 taught · BUILDING`. **Four new rules, seven negative tests, all seven catching.** |
| **3.30** | Aug 16 | **THE SCIENCE LAB IS FINISHED. 48 of 48 lessons, and ALL 25 GEORGIA SCIENCE ELEMENTS have a lesson.** Modules 6, 7 and 8 in one version. **Module 7 is built backwards from every other version of this topic** — Georgia asks her to EVALUATE a model, not build one, so she builds two and finds they disagree. **LESSON 41 NEARLY FAILED THE DISTINCTNESS TEST**, and survived only because a fourth search found a genuine match. **THE DOSING SCAN FIRED ON AN ASTRONOMY QUESTION** — the question changed, not the check. Eighteen negative tests, all catching. |
| **3.29** | Aug 16 | **QUARTER 3 BEGINS. Module 5, Sound in the Greenhouse.** **LOUD AND HIGH ARE TWO LESSONS, AND THAT IS THE WHOLE DESIGN.** **AND TWO NEGATIVE TESTS FAILED TO FAIL, WHICH FOUND TWO REAL HOLES** — nothing compared a lesson's quarter to its module's, and the reading floor averaged across courses so sixty bad prompts hid inside four hundred good ones. **The floor is measured PER COURSE now.** |
| **3.28** | Aug 16 | **QUARTER 1 OF THE SCIENCE LAB IS FINISHED.** Module 4, *Light, and What It Meets*. **THE QUARTER 1 EXAM CAN OPEN NOW** — and **it titled itself "sciencelab, Quarter 1"** the first time it could be built, putting a database id in front of a nine-year-old. **AND THE NEGATIVE TEST THAT COULD NOT FAIL WAS MADE TO FAIL.** |
| **3.27** | Aug 16 | **MODULE 3 — SIX SIMPLE MACHINES IN THE GARDEN SHED.** **FOUR OF ITS VIDEOS HAD NEVER BEEN FOUND.** **AND `check-standards` WAS DOING WHAT THE APP DID AT v3.24** — it read Herbalism only, so fifteen elements printed as "still owed" for ever. **A gap printed too large looks like honesty and is not.** |
| **3.26** | Aug 16 | **MODULE 2 — GRAVITY, AND WHICH WAY IS DOWN.** **THE LAST LESSON IS THE STANDARD ITSELF** — Georgia asks her to CONSTRUCT AN ARGUMENT. **The build failed for carrying a waiver it no longer needed**, which is exactly what that rule is for. |
| **3.25** | Aug 16 | **THE SCIENCE LAB WAS WRITTEN AND SHE COULD NOT REACH IT.** **NOTHING IN THE APP IMPORTED IT. NOT ONE FILE.** **TWENTY-FOUR CHECKS WERE GREEN THE WHOLE TIME.** **AND THIS EXACT FAILURE WAS FIXED BEFORE, AT v3.3** — what came out of it was a sentence in this log. **A sentence in a log is not a rule.** **CHECK #25, `check-delivery`.** **FOUR MORE REAL BUGS THE WIDENED DOOR EXPOSED.** **THE READING EXEMPTION IS NOW DERIVED, NOT LISTED.** |
| **3.24** | Aug 16 | **THE SCIENCE LAB HAS ITS FIRST LESSONS.** **THE FIRST BANK DRAFT HAD 42 OF 60 ANSWERS IN SLOT B AND NONE IN SLOT D.** **Check #24, and it caught me twice while I was writing it.** **⚠️ v3.25 CORRECTION: this entry said "wired in and checked". It was not wired in.** |
| **3.23** | Aug 16 | **"NOT PLANNED YET" IS GONE.** It went because it stopped being TRUE. **The FACT did not go with it**, and **the guard was INVERTED rather than deleted.** |
| **3.22** | Aug 16 | **SOCIAL STUDIES AND THE HUMAN BODY ROTATE BY DAY.** **The week shape stopped being one-size-fits-all.** **It took 16 lessons off the pile.** Negative tests found THREE holes in my own checking. |
| **3.21** | Aug 16 | **"MATH JUST SKIPS TO UNIT 6."** One rule doing two jobs. **AND MY OWN CHECK WAS THE FIRST SUSPECT — IT WAS GUILTY.** **THE SCIENCE LINK WAS DEAD.** |
| **3.20** | Aug 16 | **HER SCHEDULE OPENS THE UNIT, NOT A PAGE OF UNITS.** **THE 2ND GRADE MATHS LINK WAS DEAD AND HAD BEEN FOR MONTHS.** **No check could have caught it:** Khan answers **HTTP 200** for the dead address. **Check #23**, ten negative tests. |
| **3.19** | Aug 16 | **KHAN GRADES REACH THE RECORD.** **It is THREE subjects, not four.** `khanGrades` at DB v6. |
| **3.18** | Aug 16 | **THE GEORGIA HOURS LEDGER.** **The requirement was LOOKED UP, not remembered.** One negative test found that **ticking LUNCH was adding twenty minutes to a legal record.** |
| **3.17** | Aug 16 | **TWELVE TABS BECAME SIX.** **Her list contained one contradiction and she settled it.** |
| **3.16** | Aug 16 | **"THERE ISN'T ANYTHING ON THE WALLS" — and the check written to prevent exactly this was passing.** **The rule counted an item's `u`.** |
| **3.15** | Aug 16 | **THE SHOP AND THE GARDEN — and both diagnoses were the wrong way round.** **Check #22.** |
| **3.14** | Aug 16 | **A RE-TAKE IS NOW A DIFFERENT PAPER.** 360 simulated re-takes: **zero repeats.** |
| **3.13** | Aug 16 | **THE BACKUP WAS MISSING A WHOLE TABLE.** **Every link was correct** and I said so rather than inventing a fix. |
| **3.12** | Aug 16 | **500 PETALS FOR ONE DAY OF SCHOOL.** **Gigi's call:** reset, **she keeps what she bought**. |
| **3.11** | Aug 16 | **FRIDAY HAS A SCREEN.** |
| **3.10** | Aug 16 | **FRIDAY, AND SIXTEEN PROJECTS.** **Check #21.** |
| **3.9** | Aug 15 | **HERBALISM IS COMPLETE.** 96 lessons, 895 questions, 96 verified videos. **⚠️ v3.31 CORRECTION: left marked `state: 'building'` here, which switched off the only guard `check-standards` has.** |
| **3.8** | Aug 15 | **48 LESSONS.** A module is **a fortnight**. |
| **3.7** | Aug 15 | **THE UNITS CAME OUT.** My Courses was **hand-typed HTML never wired to data**. |
| **3.6** | Aug 14 | **BEATS, AND A TEST ON FRIDAY.** |
| **3.5** | Aug 14 | **THE FRAME.** Georgia GSE verbatim. **Two courses were called her science and covered ZERO of the eight.** |
| **3.4** | Aug 14 | **THE LESSON NOW TEACHES WHAT ITS VIDEO TEACHES.** Gigi caught it. |
| **3.3** | Aug 14 | **THE LESSON IS ACTUALLY ON HER SCREEN.** v3.2 wrote one and imported it nowhere. |
| **3.1** | Aug 13 | **A PRACTICE GATE AND AN EXIT TICKET.** |
| **3.0** | Aug 13 | Unit and quarter tests, spaced review, Gradebook. |
| **2.7** | Aug 13 | **Gigi caught a real one.** Three grade-mapping bugs. |
| **2.4** | Aug 13 | **Found why the app kept looking empty.** Vite slid to 5181. |
| **2.2** | Aug 13 | **Her two 2.00s were my bug.** 81 new questions. |
| **2.1** | Aug 13 | Greenhouse rebuilt as a room with real depth. |
| **1.0–2.0** | Aug 12–13 | The adaptive Check-In, the Khan plan, the Market, the Greenhouse, Dr. Marigold, the Journal, Today with a bell |

---

## 2. Bugs found and fixed

| Bug | Root cause | Fixed in |
|---|---|---|
| **⭐ A Course Challenge result had nowhere to be recorded** | The Khan panel had one row per unit and no row for the course-level test. **The single cumulative result in a course could not be entered at all**, so it never reached the record or the annual progress report — and the only place to put it was a unit row, which would have marked a unit she never sat as done | 3.76 |
| **A row with no unit number read as "unit 0 is done"** | `nextUnitFor` used `Number.isFinite(Number(g.unitN))`, and **`Number(null)` is `0`**. Harmless only because **no Khan course has a unit 0** — luck, not design — and v3.76 introduced exactly such a row. **Third file with the same `Number(null)` trap in two days** | 3.76 |
| **⭐ Lamar's grading ladder was reported wrong, from his own documentation** | `PROJECT_LOG.md` states his report-card scale as five bands — *"A 90-100, B 80-89, C 70-79, D 60-69, F below 60"* — and v3.74 **stated it as a fact in this log**. His running app renders **A+ 99%, A 93%, B 86%, B- 82%**: a plus/minus ladder. **A document about an app is not the app**, and his folder holds docs and no code, so **its newest page is still older than his screen** | 3.75 |
| **An absent mark came back an F, and an ungraded unit averaged in as a zero** | `Number(null)` is `0` and `Number('')` is `0`, and **both are finite**, so every `Number.isFinite(Number(x))` guard in `khanGrade.js` treated *missing* as *nought*. **An unfinished course would have looked like a failing child.** §3.6 already forbids it for rubric rows — *"an unmarked row is not a zero"* — and it was being remembered rather than enforced | 3.75 |
| **⭐⭐ Recording a Khan grade never advanced her — and it was diagnosed as "not a bug"** | `useAppStore.js` — `addKhanGrade` stored `{ subject, unit, percent, at, note }` and **wrote neither `courseId` nor `unitN`**, the two fields `nextUnitFor` requires before it will count a unit as done. **One writer in the whole app, and it wrote neither.** The Aug 23 handoff concluded *"it is not a bug — it is a missing manual step"* and queued a Planner hint as the fix; **that hint would have told Gigi to do something that does not work.** Her record holding zero grades hid it — with nothing entered, a broken writer looks exactly like a working one | 3.74 |
| **⭐ The check guarding that advancement passed green for four versions** | `check-khan-units.mjs` §6c — walked Unit 1 → 8 using **grade objects it built itself**, a shape the app had never produced, and printed *"advance one unit per grade"*. **Testing `nextUnitFor` in isolation and making a claim about the app.** The same file confesses to the identical failure at §6b, eight lines above | 3.74 |
| **Her diagnostic document disagreed with her record, and it governs all 256 lessons** | `azianna-diagnostic-results.md` said Grammar 2.15, Writing 2.45, Geometry and Measurement 2.00; her backup says **2.20, 2.70, 2.70, 2.70**. Hand-typed, never re-read off the data — **the tenth number in this project to drift.** The four that moved are the four still `settled: false`, so the §5 re-measure had been happening and the document did not know | 3.74 |
| **A lesson records `passed: true` with ZERO questions answered** | `LessonReader.jsx:577` — the finish button is never disabled, not on `allChecked` and not on `needsExtra`, and `practiceGateResult([])` returns `asked: 0, passed: true`. **`needsExtra` is computed at line 163 and `finish()` never consults it.** In her real data `sl-m2-01` scored **1 of 3, `passed: false`, `extraServed: 0`** — the gate fired and served nothing. **A row in a Georgia record that claims more than it tests** | **FOUND v3.56, NOT FIXED** — it is `attemptState`, master plan §32.2 |
| **The per-strand read-aloud breakdown is written and no screen shows it** | `useAppStore.js:1452` — `readAloudByStrand()` has **zero consumers**. The One-Page Report computes an app-wide total inline instead, and caveats only `READING_STRANDS = ['reading-comprehension', 'vocabulary']` — **hand-typed, inside a component, where no check can reach it.** So the report tells a grown-up that hearing a **grammar** question aloud "removes the reading load", and **9 of her 42 read-aloud answers are in Grammar-Usage and Writing-Strategies.** The v3.25 failure again: written, correct, unreachable | **FOUND v3.56, NOT FIXED** — master plan §32.2a |
| Diagnostic read every student **high** | A symmetric staircase over 4-choice items settles above true ability | 1.0 |
| 62 of 189 questions read **harder than they tested** | Reading load was never measured | 1.2 |
| Dr. Marigold spoke in a **man's voice**, twice | **Chrome loads voices asynchronously.** | 1.5 |
| Her **face wasn't there** | Hair painted last, over everything | 1.6 |
| **"You keep deleting her info"** | Nothing was deleted. Vite used 5181; work is filed per-address. | 2.4 |
| **Two of her strands read 2.00** | The engine's floor was 2.0; the bank's easiest question was 2.3–2.5 | 2.2 |
| **Six of nine Khan assignments were wrong** (Gigi found it) | Three band bugs, boundaries ending at `.9` | 2.7 |
| A warm-up question **reappeared in that afternoon's test** | Nothing stopped it | 3.0 |
| A third attempt re-served **9 of the 10 questions she'd just failed** | Repeats shuffled at random | 3.0 |
| **A lesson was written, delivered and invisible** | Imported nowhere. **"Placed" is not "shipped".** | 3.3 |
| **A lesson and its video taught two different things** | The video was picked *after* the lesson. Gigi found it. | 3.4 |
| **Two courses were called her science and covered none of Georgia's eight** | Nobody had read the state's document | 3.5 |
| **130 lessons planned against the 288 the schedule demands** | Nothing compared school days to lessons | 3.5 |
| **"Module 1 is already completed"** | It was 1 lesson of 6. **I checked the disk.** | 3.7 |
| **My Courses showed no courses** (Gigi found it) | **Hand-typed HTML never wired to data** | 3.7 |
| **`check-standards` reported "0 of 25"** while ten had lessons | A hand-maintained list going stale | 3.9 |
| **noembed.com "verified" a fake video id** | One stale cached body for every id. **DO NOT USE noembed.** | 3.9 |
| **`catchUp.js` checked `.done`; the store persists `.doneAt`** | Every project would have stayed on Friday for ever | 3.10 |
| **The safety scan fired on Paracelsus** | **Reworded the prompt rather than loosen the check.** | 3.10 |
| **The Check-In paid 715 petals; a school year paid ZERO seeds** (Gigi found it) | Nothing in the curriculum minted a seed | 3.12 |
| **Projects were in none of the backup paths** | My own bug from v3.10. Three silent versions. | 3.13 |
| **`check-import` claimed more than it tested** | It tested a **read** note stays read, not that a **new** note arrives | 3.13 |
| **A diagnostic re-take could serve the same questions again** (Gigi found it) | `reopenStrand()` cleared `seenItemIds` | 3.14 |
| **All four ELA strands were too thin for a re-take** | 15 items against a sitting of 8, behind a **warning** that never printed | 3.14 |
| **Twenty-one of twenty-two purchasables stood on the floor** (Gigi found it) | Nothing asked whether every **PLACE** had something on it | 3.15 |
| **The garden was a ranking of a child's own skills, in plants** | Stem height was her measured level | 3.15 |
| **Nothing on the walls or ceiling** (Gigi found it, **twice**) | **`check-room` counted an item's `u`** | 3.16 |
| **A new check read the wrong region of the file** | **Caught by its own negative test.** | 3.17 |
| **Ticking LUNCH added twenty minutes to a legal record** | **Found by a negative test, not by reasoning.** | 3.18 |
| **The 2nd grade maths link was DEAD** | **Khan serves HTTP 200 for it, so no automated check could ever have seen it.** | **3.20** |
| **Eight unit names in the app do not exist on Khan** | **The links resolved, so the wrong LABEL was invisible.** | **3.20** |
| **Her schedule opened a course index, not her unit** (Gigi found it) | `resolveBlockTarget` returned `courseUrl` | **3.20** |
| **An `exact` flag that could lie** | Computed from something other than the thing it described | **3.20** |
| **Maths skipped to Unit 6** (Gigi found it) | **One rule doing two jobs** | **3.21** |
| **My own check asserted "an exact unit" and never asked WHICH** | **Green while the app skipped five units.** | **3.21** |
| **All three elementary science links were DEAD** (Gigi found it) | **Nothing ever re-checked a URL it had already written down.** | **3.21** |
| **Reverting the shared lesson-count line passed silently** | Nothing asserted the per-course arithmetic | **3.22** |
| **A course moved out of the rotating block left two empty half-hours** | The check walked courses → days and never days → courses | **3.22** |
| **An eight-question test on a two-lesson week** | Nothing compared test size to lessons taught | **3.22** |
| **42 of 60 bank answers in one slot, and none in another** | A rule in a header that nothing tested | **3.24** |
| **A new check failed five of six lessons, and the CHECK was wrong** | **Would have forced me to pad the data dishonestly.** | **3.24** |
| **A WHOLE COURSE WAS WRITTEN AND NO SCREEN COULD REACH IT** | Every consumer imported a file named after ONE course. **24 checks green.** | **3.25** |
| **Friday judged two courses against one marker** | `furthestWeek` sorted across all courses. **Debt she never incurred.** | **3.25** |
| **The quarter exam would have opened on a two-week quarter** | "Every REGISTERED week sat" equals "every week" only for a finished course | **3.25** |
| **The interleaving rule counted a second course's first week as the 33rd of the first** | The check sliced the global week list | **3.25** |
| **Sixty Science Lab questions had never been measured for reading load** | `check-assessment` imported Herbalism's bank and called it the app's | **3.25** |
| **Projects were judged against whichever course she opened last** | 16 projects are Herbalism's and nothing said so | **3.25** |
| **A stale `coverageNote` survived the thing it excused** | **The build failed for carrying a waiver it did not need — the rule working.** | **3.26** |
| **`check-standards` printed every Science Lab element as "still owed"** | It imported `ALL_HERBALISM_LESSONS` and called it "every lesson in the app" | **3.27** |
| **The quarter exam titled itself with a raw course id** | A ternary with one course hard-coded and the id as the fallback | **3.28** |
| **Nothing compared a lesson's quarter to its module's** | The quarter picks the reading ruler. **Found by a negative test that failed to fail.** | **3.29** |
| **The reading FLOOR could not see one course written too easy** | It averaged a whole quarter across both courses | **3.29** |
| **HERBALISM SAID `'building'` WITH ALL 96 LESSONS WRITTEN — FOR TWENTY-ONE VERSIONS** | `check-standards` hard-fails only when the owning course says `'complete'`. **A state that understates is the direction that stops a check from running.** | **3.31** |
| **The rotating block could not see quarters at all** | It asserted "every weekday has a course" and never "in every quarter" | **3.31** |
| **TWO FILES CLAIMED GEORGIA'S SOCIAL STUDIES STANDARDS WERE TRANSCRIBED. THE FOLDER HELD ONE FILE.** | `check-standards` only ever imported `GA_SCIENCE_4`, so **nothing tested the claim** | **3.32** |
| **⚠️ THE ENGINE HANDED A NINE-YEAR-OLD THE WRONG PAPER** | `buildWeeklyTest` used the global 8-question size for every course. Both numbers had been per-course since v3.22; **nothing propagated them into the engine.** | **3.34** |
| **`check-assessment` measured a two-day course with a three-day ruler** | One global week shape, one global test size | **3.34** |
| **The derived reading exemption was one course wide** | Built from The Science Lab and handed out on the `sl-` prefix | **3.34** |
| **`check-curriculum-volume` held a literal `social: 0`** | Right failure, wrong reason. **A number that happens to be correct today is not a measurement.** | **3.34** |
| **A negative test MISSED — the engine's question MIX** | A shorter paper hid a changed mix behind a length assertion that stayed green | **3.34** |
| **`simulate-year` could not reach Social Studies week 1 in 170 days** | A re-take of week 3 beat the first sitting of week 33. **Extended to 260 days rather than lowering the bar.** | **3.35** |
| **⚠️ HALF THE WRITING MINI-LESSONS WERE UNREACHABLE FOR EVER** | Kind and index derived from the same number over two 36-long lists | **3.38** |
| **A NEGATIVE TEST GOT THROUGH: ripping the mini-lesson out of the Journal** | The check located the block by a comment marker and `indexOf` matches a superstring | **3.38** |
| **⚠️ THE RUBRICS AND THE GRADING WERE WRITTEN AND NO SCREEN SHOWED THEM** | The v3.25 failure one version after the check written to prevent it — `check-delivery` guards LESSONS, and a rubric is not a lesson | **3.39** |
| **THREE NEGATIVE TESTS MISSED, AND EACH FOUND SOMETHING REAL** | An assertion that read to the end of the file; one satisfied by a parameter name; and two mutations that landed in a second `readAloud` belonging to the Check-In | **3.39** |
| **⚠️ A MINI-LESSON ASKED FOR WHAT IT NEVER TAUGHT** (Gigi found it) | It showed go, see and bring, then asked for the past of run, write and grow. **Nothing asserted that a lesson can answer its own question.** | **3.40** |
| **⚠️ FORTY OF THE SEVENTY-TWO MINI-LESSONS READ ABOVE A 3.0 GRADE** (Gigi found it) | One hit 8.0, against a child whose Grammar & Usage measured **2.15**. **check-readability guards Check-In items, check-assessment guards bank questions, and this prose fell in the gap between them** — the same gap sixty Science Lab questions fell into at v3.25 | **3.40** |
| **The mini-lessons came up in no order at all** | Delivery hashed the day key, so an apostrophe lesson could land in week one. **Sentence structure never got built, which is what Gigi actually noticed.** | **3.40** |
| **A one-argument call would have handed her LESSON 1 FOR EVER** | `miniLessonFor` defaults her progress to an empty list, and every other assertion stayed green. **An optional argument that must always be passed is a rule nobody enforces.** | **3.40** |
| **A check tested a word on the screen instead of the thing rendered** | Deleting the rubric from her screen stayed green because the panel's own sentence still said "Read the rubric first" | **3.40** |

**Two of the v3.0 bugs were found by `simulate-year.mjs` on its first run, not by reasoning.**

**Not every hunt ends in a fix.** At v3.13 every link was correct and I said so rather than shipping a guess.

**A fix can expose a second bug.** v3.14, v3.22, **five times over at v3.25**, and again at v3.34.

**The same complaint arriving twice is the strongest signal there is.** **When a person repeats a complaint, check the CHECK before checking the content.**

**A contradiction in a specification is a question, not a judgement call.**

**⭐ THE ELEVENTH, Aug 18 — AND IT IS A NEW CLASS AGAIN.** *"i don't see how to create an export."* **It took six exchanges to find the export control, and I was giving wrong directions for four of them** — because nothing on screen says "export". The Settings group holds **"Bring her work here"** and **"Backup & settings"**, which read as one page so scrolling never reveals the second, and the control is called **"⬇ Download backup (.json)"**. Three words for two directions of one idea. **Every check was green: `check-links` asserts import, export and backup are all reachable under Settings, and they are. Reachable is not findable.** The thing this guards is the only copy of a child's work, and there is no cloud behind it. **A control whose label does not say what it does is the same fault as a stale comment — it sends the reader somewhere that is not there.**

**AND THE FILE THAT BUTTON OFFERED WAS STALE AND WOULD HAVE DONE HARM.** `public/her-latest-export.json` held the Aug 13 export. Her live database held four strands **mid-re-measure** — geometry and measurement reading **2.70** rather than the old floored 2.00. *(Corrected at v3.56: this read **2.69**. The stored value is `2.6999999999999997` and every screen formats with `.toFixed(2)`, which **rounds** — so the app has always printed 2.70. Somebody truncated it by hand on the way from the screen into this file, and the wrong figure then travelled into the session prompt. **The ninth hand-typed number in this project to drift, in the sentence describing a hazard about stale numbers.**)* The merge keeps whichever side answered more questions, and the old file had more. **Pressing the button the app was recommending would have overwritten the re-measure and put 2.00 back.** Swapped at v3.55; the old file is preserved in `_to_delete/v3.55-old-export/`. ~~**The button still does not say how old its file is relative to what is already here, and it should refuse when it is older.**~~ — **DONE at v3.62**, and the merge rule underneath it was fixed at v3.60 and again at v3.66. See §33.1: it was wrong in BOTH directions inside twelve hours, and `seenItemIds` is what finally told "later" from "different".

**GIGI HAS NOW FOUND TEN REAL BUGS BY USING THE APP WHILE EVERY CHECK WAS GREEN** — four in v3.20–v3.24; **two at v3.40 that were the same class as each other**, content that nothing measured, in a gap between two checks that each guarded something else; and **one at v3.42 that was a third class again — a decision, correctly made and correctly recorded, that nobody revisited when the thing it was about changed.** The check did not have a gap. **It had a list.**

**AND THE BIGGEST ONE OF ALL WAS FOUND BY READING THE DISK RATHER THAN THE DOCS.** At v3.25 the log said Module 1 was wired in and no screen could open a lesson. **Verify against the disk, not against what the app says about itself.** v3.32 is the same fault in a document; v3.39 is the same fault one version after the check written to stop it.

**NEGATIVE TESTS HAVE NOW FAILED IN BOTH DIRECTIONS.** Four times in v3.25–v3.30 a mutation **could not fail** because the data happened to be complete — recorded as unrun, never as green. And five times a mutation **ran and missed**: v3.34, v3.38, twice at v3.39, once at v3.40. **Every one of those misses found something.**

---

## 3. The thirty-eight checks

**Each was written *after* a real failure it would have caught, and each was negative-tested by reintroducing the bug.**

| Check | What it proves | Written because |
|---|---|---|
| `check-sources` | Imports resolve, brackets balance | Nothing could be compiled |
| `check-imports` | Every import points at a real file **and a real export** | A renamed function white-screens a tab nobody opened until she taps it |
| `check-jsx` | Every tag opens, nests and closes | An unclosed tag balances its braces and stops the app loading |
| `verify-itembank` | Questions well-formed; **every strand holds two full sittings** | Her two 2.00s; then four ELA strands too thin for the re-take rule |
| `check-readability` | Every diagnostic item reads at or below the level it tests | 62 items read harder than they tested |
| `check-placement` | Everything purchasable has a place | The store sold things with nowhere to put them |
| `simulate-diagnostic` | 240 students land within a grade; **360 re-takes never repeat** | The engine read every student high |
| `verify-economy` | Balances, merges, rank gates | The Check-In out-earned a school year |
| `check-avatar` | Nothing is painted over her face | Her hair covered her face for three rounds |
| `check-hooks` | Every hook runs before every early return | Caught a crash I'd just written |
| `check-schedule` | Georgia's 4.5 hours · **a break is never instructional time** | I claimed four hours and built 3h20; then lunch entered a legal record |
| `check-links` *(rebuilt v3.42)* | Every block that names a subject opens it, **on every day Monday to Friday** · **it opens the RIGHT course, and names it** · **the label and the link agree** · **a block only hands its time to the garden once she has FINISHED, tested in both directions with made-up progress** · **the unlinked exemption is DERIVED from written lessons, so it expires by itself** · **TodayView must pass her progress AND the date in, counted with a real parenthesis scan** | Her day named subjects and made her go find them. **And then it did it again for twelve versions** — Science and Social Studies returned null under a comment whose reason had expired, while a hand-written exemption list kept the check green |
| `check-room` | **Every wall carries something MOUNTED on it** | "Nothing on the walls" — **said twice** |
| `check-import` | **Every exported table is read back** | A whole table shipped that the import never read |
| `check-yearplan` | **A ceiling gets its year** | **INVERTED at v3.23.** |
| `check-assessment` | **2,560 questions across four courses**, caps and floors **per course**, exemption **derived per course** · **and the paper the engine builds is the size that course declares** | A module written too easy could hide in another course's average; **then the engine built an 8-question paper for a course declaring 5** |
| `simulate-year` | **260 school days played out** · **a first sitting is never starved by a re-take** | "Spaced review" is a claim about months |
| `check-standards` | **Both subjects**, every element owned once · **the owning course's `kind` must match** · **a dropped element must be DECLARED, and a stale declaration fails** | Two courses were called her science and covered none of the eight |
| `check-curriculum-volume` | School days against lessons · **holes declared per DAY** · **a rotating course that ends early says so** · **full lessons may not say `'building'`** · **TodayView must pass progress in** | 130 planned against 288 needed; then one word switching off another check's only hard failure |
| `check-videos` | Every lesson has a video, verified, dated, its search recorded | "Every lesson has a video" was an intention, not a fact |
| `check-projects` | One project per module · **Friday is judged per course** | A wrong field name; then two courses shared one marker |
| `check-garden` | Seed → flower on days worked; **never reads a measured level** | The garden drew a ranking of a child's skills as plant heights |
| `check-khan-units` *(#23)* | **A unit link is never its own course front page** | **Four bugs Gigi found with 23 checks green** |
| `check-sciencelab` *(#24)* | **A video that shares nothing with its lesson fails** · **no answer slot over 40%** · **a lesson agrees with its module about its quarter** | *"…just didnt add random videos"*; 42 of 60 in one slot |
| `check-delivery` *(#25, extended v3.41)* | **A written lesson sits in a week, has questions, and is findable app-wide** · **every screen reads the app-wide door, asserted by reading source as TEXT** · **and since v3.41, THE VIDEO RENDERS ABOVE THE LESSON PROSE** — between the hook question and Part 1, asserted by the **position of rendered elements with all comments stripped**, measured **only inside the component that renders**, never by looking for the word "video" | **A whole course was written, logged as shipped, and unreachable.** **And then a lesson was reachable in an order she could not read** — the video sat under every word of prose for a child whose Reading 3.46 is a listening score. **Reaching a lesson is not the same as reaching it in an order she can read.** |
| **`check-writing`** *(#26, rebuilt v3.40)* | **A lesson may only ask for what it gave her — every content word of a closed answer must appear in the lesson, and fewer than forty closed tasks fails the build** · **every mini-lesson measured against its stage's reading cap, titles capped at seven words so the taught-word exemption cannot be widened** · **the sentence stage is first and unbroken, and paragraphs are not taught before sentences** · **the lessons come up in ORDER, driven by days she has written, and today's entry does not move today's lesson** · **JournalView must pass her progress in, counted with a real parenthesis scan** · **the Journal has no input, no score, no submit — read as text** · rubric wording, read-aloud, two research papers, the grading arithmetic · **and the graded pieces are markable, readable by her first, and survive a merge** | **36 of 72 mini-lessons were unreachable**; **a negative test walked through a comment-marker search**; **the rubrics were written and no screen showed them**; and then **Gigi found a lesson asking for what it never taught, forty of them pitched years above her, and no order at all** |
| **`check-lesson-prose`** *(#27, new v3.70)* | Every lesson reads at or under its quarter's cap — and the known breaches are an exact, ratcheted list | The caps lived in seven module header comments and nothing measured them. **Same failure as the first `elaItems.js`, one layer down.** 37 of 256 were over, 18 of them one contiguous Herbalism Q4 block written before the policy existed |
| **`check-version-stamp`** *(#28, new v3.70)* | **The version number says the same thing everywhere** — `buildStamp.VERSION` is its own newest `CHANGES` entry, the changelog is unique and strictly descending, and all three version claims in this log agree with it · **and the script counts in the header, the §3 heading word and this table's row count all match the scripts on disk** | **The third time the same drift happened.** This file already carried two hand-written correction notes about it — v3.56 (*"two versions of the truth, three lines apart, in the file whose job is to prevent that"*) and v3.60 (*"the same drift, in the file that had just been corrected for it"*) — and at v3.70 the header read v3.69 while the table read 3.70. **Two notes in a file are not a check. Nothing had ever read these numbers.** |
| **`check-skills`** *(#29, new v3.71)* | **The skill catalog is internally consistent** — ids unique and kebab-case, every skill fully described, **a source term owned by exactly one skill**, `lessonCount` an integer ≥ 2 or null **with a written reason**, no strand silently empty, **held and retired terms not also live**, and `skillForTerm()` agreeing with the table and still rejecting vocabulary | **A wrong entry here does not fail loudly.** It produces a remediation set aimed at the wrong thing months later and nothing on any screen says so. 156 candidates were cut to 20; the catalog is what Phase 2 tags 2,560 questions against |
| **`check-publish-safety`** *(#30, new v3.72, extended v3.77)* | **Nothing of hers gets published, and now nothing of hers gets COMMITTED** — every JSON on disk identified structurally and checked against `.gitignore`, the specific ignore rules asserted **by name** so they cannot hold by luck, and the Netlify build command asserted to run the checks **before** it publishes · no file in `public/` or a built `dist/` carries her export, and **the two guards that keep it that way are asserted by reading the source with comments STRIPPED** | **`public/her-latest-export.json` was her real data** — learner name, all nine measured levels, 74 answers, both journal entries — and **everything in `public/` is copied verbatim into `dist/`.** The first upload anywhere would have put a nine-year-old's reading level at a public address |

| **`check-khan-advance`** *(#31, new v3.74)* | **Recording a grade actually moves her on** — a row from the app's own writer carries `courseId` and `unitN`, makes `nextUnitFor` advance, and walks every gradeable course end to end to the Course Challenge · **the store still calls the writer and has not gone back to hand-building a row** · the unit is picked, never typed · **a pre-v3.74 free-text row is still ignored, never guessed at** · **the ladder is Lamar's seven — A/A-/B+/B/C/D/F — and the writer refuses anything else** · **every letter the on-screen guide offers is one the writer accepts**, so the screen cannot tell Gigi to pick something unsaveable · **the panel renders a row-level picker and not a `<select>`** · Azianna still gets a band and never a letter, and below 70% never reads as *"nearly there"* · **a Course Challenge never marks a unit done, is never averaged with the units, and is refused for a course Khan built none for** · **the unit-done rule asked directly of BOTH files that state it, on seven shapes of row** · **and it builds no grade object of its own, which is the entire point of it** | **`addKhanGrade` never wrote the two fields `nextUnitFor` requires, so recording a grade advanced nobody — and `check-khan-units` §6c passed green while it did, because it pushed grade objects IT INVENTED.** A shape the app had never produced, printing *"advance one unit per grade"*, which is a claim about the app. **The same file already confesses to that exact mistake one section higher.** Testing the shape of an answer instead of the answer, twice, eight lines apart |

| **`check-word-study`** *(#38, new v3.90)* | **Her spelling and vocabulary words are really hers** — 32 weeks, ten of each, 320 and 320 counted rather than trusted · **every word traces to a lesson the week table schedules in ITS OWN WEEK**, and is still in that lesson's prose or its `words:` array · no word is used twice, in either list or across the year · every spelling word is 4–9 letters, at most two syllables, and **not a proper noun** · and the week count matches the schedule, so it cannot describe a year she does not have | **Gigi's call: "pull from the 256 lessons."** So the list is not a stack of words beside her week — **it IS her week**, and a freeze is only safe if something watches it. ⚠️ **It found a real collision on its first run** (`record`, `step` — the generator filtered against a vocabulary set that was still being built) **and its second run exposed a proper-noun rule that called `garlic`, `ginger` and `seeds` names.** ⚠️ **AND IT ASSERTS NOTHING ABOUT REACHABILITY — there is no screen yet** |
| **`check-diagnostic-record`** *(#37, new v3.86)* | **Her diagnostic document says what her record says** — §1 holds **one row per strand, nine of nine**, and a strand missing from the table **fails rather than exempting itself** · every §1 level equals `level.toFixed(2)` on her record, the way every screen in the app formats it · §1d's read-aloud sentence counts what her record counts · **the export is found structurally and the one with the MOST ANSWERS governs**, never the newest file date · **§1b and §3 are deliberately NOT covered**, and it says so in its own output · **and when neither file is on the computer it prints *NOTHING WAS COMPARED* and never the word PASSED** | **Third drift in the one file that governs the reading level of all 256 lessons** *(master plan §30)*. Aug 24: Grammar 2.15 against a record saying 2.20 — **"the tenth hand-typed number in this project to drift."** Aug 26: Measurement 2.50 against 2.44. **Aug 26 again, one section down: §1d said 54 of her 86 answers when her record held 90 — and nobody had noticed that one at all.** ⚠️ **It is the first check here that can pass without testing anything**, because her export and her assessment are both kept out of a **public** repository on purpose. Gigi's call, and the honesty is the price of it |
| **`check-book-report`** *(#36, new v3.82)* | **The book report is scheduled, stepped and actually written** — no step ships as a bare label · **no due date lives on the piece definition** and nothing in the scheduling reads a clock · a step is never offered before it opens and every step fits inside its quarter · what she has ticked beats what the week suggests · **the final step cannot be ticked on an empty draft** · notes and draft stay two boxes · **a newer empty backup can never erase her writing** · and Today's Planner still knocks | **They existed and nothing ever said it was time.** Four a year since v3.38, no schedule, no prompt — **zero writing marks on her record.** And there was nowhere to type: four completed book reports could have sat on a Georgia portfolio holding nothing she wrote. **The artifact IS the record, and a checkbox is not an artifact** *(Lamar's app, Aug 15 2026)* |
| **`check-strand-lanes`** *(#35, new v3.81)* | **Each strand walks its own units and nothing skips** — every unit in **exactly one lane** · each lane **starts at its own first unit and runs in order** · a strand with no lane **falls back and says it fell back**, never gets an invented one · themed reading units are never split · **a finished LANE is not a finished course** and does not unlock the Course Challenge · and the lane still matches the Khan unit name it was read off | **The unit-order rule was sending her weakest strands to her strongest one.** Measurement & Data 2.50 is Unit 6, Geometry 2.82 is Unit 8 — and all three maths strands were handed **Unit 1, "Add and subtract within 20"**, which teaches Numbers & Operations at 3.48. **And the fix is one step from being the v3.20 bug again**, so the difference is asserted rather than described |
| **`check-reading-check`** *(#34, new v3.80)* | **The reading check measures reading, and says which kind** — `unaidedPercent` is **null and never 0** when she was read to · **reading a passage aloud marks EVERY question on it**, the cautious direction · the flag is written at **both** row sites, not one · the passage stays on screen beside its questions · every passage is measured against the Quarter 1 cap · the answer moves around · **a reading check is never a Khan grade** · one letter ladder · and the screen never shows her the words *needed help* | **Khan built no test for this course** — 77 links on the rendered page, zero assessments — so `ela2` carries `graded: 'parent'` and the grade came from Gigi by hand. **And 63% of every answer Azianna has ever given was read aloud to her.** A reading test that does not record which is which produces one more listening score wearing a reading score's name, and it would look like it had filled the blank |
| **`check-lesson-gate`** *(#33, new v3.79)* | **The block opens ONE lesson and it is the first unfinished one** — asserted through the same functions the app calls, never a lesson row built by the check · **all 256 lessons sit in exactly one week**, so none can become unreachable · **an abandoned lesson is the next thing she is given**, not the thing she never sees again · finished lessons stay open for ever, the road ahead does not · **the lessonId survives the trip from block to screen**, read as text · and `db.js` may not go back to calling a finished lesson an opened one | **A menu of ninety-six is not an instruction.** She was skipping ahead because the screen offered her the whole year and nothing said which one was hers. **And the two rules this separates — "first unfinished" and "the one after her last" — agree for every child who never skips**, so nothing in her real record would have told them apart |
| **`check-annual-report`** *(#32, new v3.78)* | **The document Georgia asks for says only what it can prove** — all five statute subjects present and named as the statute names them · **ungraded is null with a reason, never 0 and never F** · **coverage and hours cannot move a letter, asserted by running the report twice with the coverage changed** · a grade is the mean of the components listed under it and nothing else · **the Check-In moves no grade (§3.10.6)** and growth is reported on its own instrument (§3.10.8) · enrichment is never counted as one of the five · the letter comes from the one ladder in `khanGrade.js` · and the panel derives every figure at render rather than storing it | **It is the one document that leaves the house.** A grandmother files it and it answers for a child's year — and a grade is a single number standing in for that year, so it is the thing most able to lie quietly. **All three ways it can go wrong have already happened in this family of apps:** coverage counted as performance *(Gigi caught it in Mission Control)*, ungraded averaged as zero *(three times in two days here)*, and a hand-typed number drifting from its source *(ten times)* |

**Two checks print instead of failing.** `check-standards` and `check-curriculum-volume` print the **full gap on every run** and fail only on a **false completeness claim**. **Three courses make that claim**, and the check allows all three only because **all 61 Georgia units really are taught**.

### The thirty times a check was itself wrong

30. **⚠️ A RULE WHOSE FAILURE COULD NOT BE OBSERVED, SO NOTHING WAS PROTECTING IT — v3.76.** Two negative tests against *"a Course Challenge is not a unit test"* came back **green, and both were right to.** One deleted the null guard from `countsAsUnitDone`, the other deleted the kind guard, and **neither changed a single observable answer** — the two guards are redundant with each other, and a row with no unit number lands on *"unit 0"*, which no Khan course has. **Observed through `nextUnitFor` alone, every wrong answer that rule can give still yields the right unit.** So the check was not passing because the rule held; it was passing because **the rule could not be seen either way**, and the redundancy is defence only while both halves are still there. That is #20's shape — *"green for a reason that had nothing to do with the thing being true"* — arriving as a rule that is genuinely correct and genuinely untested. **Fixed structurally, per rule 11: the predicate is exported and asked directly**, on seven shapes of row including one the writer refuses to produce but an imported backup could carry, and asked of **both files that state the rule**, because it is written twice to avoid an import cycle. **The lesson: a redundant guard is not a tested guard, and "the output was right" is not the same as "the rule held."**

29. **⚠️ TWO NEGATIVE TESTS THAT PROVED NOTHING, ONE OF THEM BECAUSE IT NEVER RAN — v3.75.** *(a)* A mutation nudged the `B` band from 83 to 85 to see whether `check-khan-advance` would notice. **It correctly did not** — the evidence is five points off a screenshot, and neither 86 nor 82 changes letter when the boundary between them moves. The test was worthless, but **the check was right**, and the useful finding was the gap itself: **B's floor is only known to be 83..86, and the rungs between the proven points are convention.** It now says so on every run rather than printing five ticks and letting the whole ladder look confirmed. *(b)* The other mutation deleted a line **by replacing a string that was not in the file** — wrong indentation — so **nothing was mutated and the check passed for the most boring reason possible.** That is *"a mutation that did not mutate"*, **#27 on this list**, and v3.68 before that, where *"the mutation declared `setKind` and never called it"*. **Both were redone with an assertion that the mutation actually applied**, which is the only reliable fix and should have been standing practice after #27. **The standing rule now: a negative test asserts its own mutation landed before it believes the red or the green.**

28. **⚠️ A CHECK THAT INVENTED THE DATA IT TESTED, IN THE FILE THAT ALREADY CONFESSED TO IT — v3.74.** `check-khan-units` §6c walked the maths course Unit 1 → 8 and then the Course Challenge, and printed *"advance one unit per grade."* It did that with `grades.push({ courseId: 'math2', unitN: i })` — **grade objects the check built itself, in a shape the app had never once produced.** `addKhanGrade` wrote neither field, so **no grade Gigi could enter would ever advance Azianna**, and this check said it would, green, for four versions. **The confession is eight lines above it, at §6b:** *"THIS CHECK WAS THE FIRST SUSPECT AND IT WAS GUILTY. The version above asserted the block opens an exact unit and never once asked WHICH. It passed, green, while the app skipped five units. A check that tests the shape of an answer and not the answer is how a bug ships with 23 checks passing."* **The same file, the same failure, one section lower, written by the session that had just written those words.** This is #17's *"a note that says more than its assertion tested"* and #2's overstating, in their sharpest form yet: **the check was not testing the app at all — it was testing one pure function and reporting on the app.** Fixed structurally, per rule 11: the row-shaping moved to `src/lib/khanGrade.js` and **both checks now record through the writer the app itself uses**, so a check row and a saved row cannot be different shapes. **The lesson is not "be careful with fixtures."** It is that **a fixture is a claim about what the app produces**, and nothing was checking that claim.

1. **Wrong on first write, harmlessly.** `check-avatar` failed on its own documentation.
2. **Overstating — v3.13.** **An overstating check is worse than a missing one.**
3. **A warning doing a check's job — v3.14.**
4. **Measuring the wrong quantity — v3.16.** Written to stop Gigi's v2.1 complaint recurring — **and it recurred anyway.**
5. **Reading the wrong region — v3.17.**
6. **Reporting into the void — v3.19.**
7. **Testing the shape of an answer instead of the answer — v3.21.**
8. **Calibrated so strictly it would have made the data dishonest — v3.24.**
9. **TESTING THE DATA AND CALLING IT THE PRODUCT — v3.25.** **The most expensive: it let a whole course ship invisible.**
10. **REPORTING A GAP TOO LARGE — v3.27.** Wrong in the safe-looking direction, which is why it survived.
11. **A REGEX THAT COULD NOT COUNT PARENTHESES — v3.31.** Four red lines on a screen that was right. **Checked the check before the content.**
12. **COMPARING A FACT WITH ITSELF — v3.33.**
13. **CLAIMING FOUR THINGS AND TESTING ONE, TWICE OVER — v3.39.** A loop rescued by one template literal; a region read to the end of the file; an assertion satisfied by a parameter name.
14. **MEASURING NOTHING AT ALL, AND THEN MEASURING WITH THE WRONG RULER — v3.40.** No check had ever read the mini-lesson prose. And the first ruler written for it split sentences only on full stops, so an example list read as one 26-word sentence and flagged 57 of 72 — **a ruler that wrong would have forced the prose to get worse to satisfy it.** Corrected to measure per field, with `·` `→` and `:` ending a unit. **Correcting a check's model is not loosening it.** Then a fifteenth: **a check that looked for the word "rubric" was kept green by the panel's own sentence "Read the rubric first" while the rubric itself was deleted.**

15b. **A CAVEAT LOSING AN ARGUMENT WITH A NUMBER — v3.55.** `isPinnedAtFloor()` has been right since v3.14 and `ceilingNote()` has said so in prose the whole time: Geometry 2.00 and Measurement 2.00 are the **floor of the item bank**, not scores. **And the figure printed beside that prose still read "2.0".** Nothing was wrong with the logic; the render told a different story from the check, and **a number wins an argument with a caveat every time.** The framework's word is *Inconclusive — too little evidence to judge, which is not the same as failed.* **A check that is right and a screen that is wrong is not a passing check** — `simulate-diagnostic` now fails the build if the word is ever replaced by a digit.

15c. **A CORRECT DECISION, RECORDED AS PERMANENT — v3.55.** Gigi declined a diagnostic re-take on Aug 13 because school had started Aug 3 and Azianna was behind. **The decision was right and cost nothing** — a ceiling cannot misplace her, because every Khan course starts at Unit 1. **It went into the master plan under "Settled and not to be reopened."** That is bug #10 of the ten she has found by using the app, in the log's own words: *"a decision, correctly made and correctly recorded, that nobody revisited when the thing it was about changed. The check did not have a gap. **It had a list.**"* Now `RE_DIAGNOSTIC` carries her words, her date and her reason, **plus a trigger that is an EVENT** — once she has read the Human Body modules that teach the floored skills, the teaching has happened and re-measuring costs no school days.

15. **FAILING TWENTY CORRECT ITEMS WITH A PATTERN WRITTEN TOO WIDE — v3.53.** The new body-image fence matched `too big` and `too small`, which in a course built on measuring things are maths feedback about a **number** — *"Ten times too small"*, *"Undissolved bits are too big for the holes"*. **A check that fails correct content teaches whoever hits it to reach for the check instead of the content**, and this file had already done that at v3.43 (twice) and v3.44. Narrowed to weight words with a **person** as the subject; *big* and *small* left out of the pattern entirely, because this course cannot function without them.
16. **SATISFIED BY THE DATA RATHER THAN BY THE ENGINE — v3.52.** The warm-up re-meeting ratio was negative-tested by the exact mutation it exists to catch, and **stayed green**: the overdue backlog fills all three slots every morning, so the branch never fires. **5 first meetings in 777 slots cannot tell a working cap from a broken one.** It prints as **UNRUN, not passed** — the first assertion in this app to report itself that way on a normal run.

22. **A CHECK THAT FAILED CORRECT CONTENT SIX TIMES ON ITS FIRST RUN — v3.62.** The new used-but-unimported scan found `practiceGateResult()`, `gradePiece()` and `ceilingNote()` inside **`buildStamp.js`** — 900 lines of changelog prose that quotes function names by design. `blank()` keeps the insides of strings on purpose, because import specifiers live in them. **Fixed at the CAUSE with a `codeOnly()` pass rather than exempting one file**, because an exemption that cannot expire is a hole. **The fifth time a check in this project has failed correct content**, after v3.43, v3.44, v3.53 and v3.56.
23. **A CHECK HOLDING ITS OWN COPY OF ANOTHER SCREEN'S LABEL — v3.62.** `check-links` asserted the literal `"Download backup"`. Renaming the button to *"Export / download backup"* — the §32.7 findability fix, a **correct** change — turned it red. Two hand-typed copies of one label is rule 20 waiting to happen, and the failure mode is nasty: a screen telling a grown-up to look for a control whose name has changed. **It reads the export button's real label now and asserts the instructions quote it** — rename one place and it goes red naming both; rename both and it stays green.
24. **AN ASSERTION ANCHORED ON A FIELD NAME INSTEAD OF THE SCREEN — v3.61.** The retrieve-beat position check compared `<RetrieveBeat` against `lesson.hook.text` — whose **first** appearance is inside `lessonChunks()`, the read-aloud function hundreds of lines above any JSX. It was asking whether a tag sits below a function definition, which is not a question about the screen. **Rule 21, in the assertion instead of the app.**
25. **⚠️ THE ASSERTION THAT MATTERED MOST DID NOT BITE — v3.61.** *"A lesson may only ask for what it gave her"* is the oldest rule in this project, and its new fixture gave every question the same box, `seen` and due date — so when the exclusion was deleted the picker still happened to reach for the taught questions first and **the leak never appeared. It passed on broken code.** Found only by removing a neighbouring assertion to see whether this one could speak for itself. It could not. **The fixture is now stacked AGAINST the rule.** A test the bug can walk past is not a test.
26. **A CHECK THAT PUNISHED THE COMMENTS EXPLAINING ITS OWN RULE — v3.63.** The Play-tab screen check searched the whole file for `locked`, `greyed` and `weakest` — and found them in the **header comment recording Gigi's decision**, plus a helper named `weakest`. **Then the fix had its own bug:** it stripped the comments and sliced at an offset computed on the **unstripped** text, so every assertion below read the wrong region and reported nonsense. Strip first, find the render second, on one string.
27. **⚠️ TWO FIXTURES THAT COULD NOT HAPPEN, AND A MUTATION THAT DID NOT MUTATE — v3.66 to v3.68.** *(a)* A load-guard fixture set `freshness.newAnswers` to 14 while every table said 0 new — two numbers `previewImport` computes **once** and uses in both places. It passed while staleness read the first and failed the moment staleness read the second, **for the wrong reason**. *(b)* The empty-`seenItemIds` guard was tested with a bare row holding `asked: 1` losing to a 12-item reading — the right answer with or without the guard, so **deleting the guard left the check green**. *(c)* The journal picker test added `const [kind, setKind] = useState(...)` and **never called `setKind`**, so the assertion — which looks for a call — correctly found nothing to object to. **A mutation that does not restore the bug proves nothing.** All three rewritten until they bit.

21. **AN ASSERTION SATISFIED BY A NAME, FOR THE THIRD VERSION RUNNING — v3.59.** Two of the eight screen checks came back green on a deleted feature. *(a)* One looked for `VerdictPill` and found the component's **own definition**, forty lines above every place it renders — so removing all of them changed nothing. *(b)* The other looked for `blockedReason`, which sits in two `filter()` calls at the top of the same file whether or not a single refusal ever reaches the screen. **This is v3.39's failure — an assertion satisfied by a parameter name — in its third consecutive appearance**, after v3.56's `.includes()` substring and v3.57's grep-instead-of-call. **The rule, stated so it stops recurring: a check on a SCREEN asks what RENDERS — a JSX tag, an element position — never what the file mentions.** Both now point at `<VerdictPill` and `{p.blockedReason}`.

20. **AN ASSERTION THAT RAN AFTER THE REPORT — v3.57.** The Batch A checks were appended to the end of `check-assessment.mjs`, which put them **after** the block that prints `errors` and calls `process.exit(1)`. They executed. They pushed onto an array nobody read again. **The check passed no matter what they found**, and the run looked completely normal — the summary printed, the notes printed, green. Caught by reading where the file ENDS rather than by reading what it PRINTED, and only because a note that should have appeared did not. **This is the fourth time in two versions that an assertion has been silently switched off rather than failing** — the `[^)]*` scan, the `.includes()` substring, the version regex that broke on its own fix, and now placement. **The common shape: every one of them looked green for a reason that had nothing to do with the thing being true.** A check must be positioned where its failure can be reported, and a check whose notes do not appear in the output is not passing — it is not running.

19. **AN ASSERTION SWITCHED OFF BY THE IMPROVEMENT IT EXISTED TO SUPERVISE — v3.56, and it happened TWICE in the same file.** *(a)* The export-version assertion parsed a literal out of `version: 7`. The fix for that line was to replace the literal with the constant `BACKUP_VERSION` — **so the regex matched nothing, the parsed value came back `undefined`, and the entire assertion was skipped.** Green, on the exact change it was written to watch. *(b)* The preview assertion asked whether `data.writingMarks` appeared **anywhere inside** `previewImport`. **Two negative tests walked straight through it**, because deleting the reported field left the name sitting on a neighbouring line. It reads the **return block** now — reading a table and not reporting it is the same outcome for the person holding the mouse. **Three separate assertions in one version have been switched off by an improvement rather than failing on one**, which is a different failure from #2's overstating and #17's over-claiming note: *an assertion that a correct change can silence is a tripwire pointing the wrong way.* Every one of them now follows the thing rather than a spelling of it.

18. **A CHECK THAT GREPPED A COMPONENT INSTEAD OF CALLING IT — v3.56.** The first version of the finish-screen assertions looked for the string `gate.correct === gate.asked` inside `LessonReader.jsx`. **Delete the branch that tells three-of-three from one-of-three and the check stayed GREEN**, because that same string appeared two lines further down in a different expression. **Two of six negative tests came back green.** A third was worse: it gated on `/markLessonRead\([\s\S]*?passed:/` in the store, and the store does not contain that — `markLessonRead` takes `practice` as a parameter and the caller builds it. **So the assertion was skipped entirely, the false "nothing is written down" sentence was put back, and nothing fired.** *An assertion behind a condition that is never true is not a passing check; it is an absent one.* The fix was structural, per rule 13: the decision moved into `src/lib/lessonFinish.js` as a pure function, and `check-delivery` **calls it** on a real lesson with made-up answers. **Eleven negative tests now, all eleven catching.** And a twelfth mutation was itself wrong — it replaced a string with a prefix of the longer one, so the two branches genuinely still differed and the check was right to stay green. **Checked the check, then checked the test.**

17. **A NOTE THAT SAID MORE THAN ITS ASSERTION TESTED — v3.56.** Three of the eleven negative tests on the new export guard found something, and **all three were in the check, not the code.** *(a)* The regex looking for the passcode strip used `[^)]*` and **stopped dead at the arrow argument** in `meta.filter((m) => …)`, so it reported a strip that was plainly there as missing. *(b)* The import assertion used `.includes()`, which is **satisfied by `data.writingMarksXX`** because the old name is still inside the new one — the mutation written to break it did not, and only a third mutation, renaming outright, revealed why. It is matched on a word boundary now. *(c)* And the worst of the three: **the block printed "the parent passcode is stripped from meta" out of a bare `if (policy parsed)`, while the assertion beside it was failing on the same run.** The check said FAILED and the notes above it said the thing was fine. **A note that says more than its assertion tested is the same class of lie as a check that overstates — #2 on this list, thirty-nine versions later, wearing different clothes.** Notes now print only from assertions that actually passed.

**And a negative test can be untestable, which is not the same as passing.** Four times in v3.25–v3.30 a mutation changed nothing because the data happened to be complete. **Every one was recorded as unrun rather than green.**

**And it can MISS, which is not the same as either.** Five times, v3.34 to v3.40. **Every time the check was rebuilt, not the test reworded** — and at v3.39 one miss was the TEST's fault: **a mutation that lands in the wrong function is not a test of anything.**

**v3.31's caveat still stands:** the Garden & Projects label has never been observed on her real data. Tested against synthetic weeks; every case passes. **Not claimed to have been seen.**

---

## 4. Decisions worth not relitigating

*(all earlier decisions stand — added at v3.20–v3.40)*

| Decision | Why |
|---|---|
| **A block opens the UNIT, never the course front page** *(v3.20)* | A course index is a page a nine-year-old has to read and search |
| **`confirmedOn` and `renderedOn` are two different claims** *(v3.20)* | A correct slug on a stale base is still a dead link |
| **A dead URL is written down, never just deleted** *(v3.21)* | Four are on the list with the date opened and what came back |
| **No automated check can verify a Khan link is alive, and the check says so** *(v3.20)* | **Only a rendering browser knows.** |
| **Her level chooses the COURSE; the course chooses the UNIT** *(v3.21)* | One rule doing two jobs put her on Unit 6 |
| **A flag must be derived from the thing it describes** *(v3.20)* | **Applied again at v3.39:** a mark stores 1–4 per rubric row, never a percentage |
| **The week shape is per course** *(v3.22)* | This is what let a two-day course exist at all |
| **A rotating block is resolved at render time** *(v3.22)* | **A name Gigi typed herself always wins.** |
| **A quarter with no course is a hole unless DECLARED** *(v3.22)* | **Per DAY since v3.31** |
| **A guard is inverted, not deleted, when the world changes under it** *(v3.23)* | |
| **A warning she cannot act on is a wall, not a warning** *(v3.23)* | |
| **If no video exists that teaches a lesson, the lesson is not distinct enough** *(v3.24)* | Fired for real at v3.30 and v3.37 |
| **The right answer must move around, and it is a CHECK** *(v3.24)* | **PER MODULE since v3.25** |
| **A check that pressures you to falsify the data is worse than no check** *(v3.24)* | **Fired at v3.40**, when the first reading ruler flagged 57 of 72 |
| **An exemption fails once it is no longer needed** *(v3.24)* | Fired at v3.26 and v3.30 |
| ~~A VIDEO MUST FIT THE SYSTEM CONCEPT OF ITS COURSE~~ *(v3.45, **withdrawn v3.46**)* | **Gigi's call.** The ceiling was an inference from §10.2, not a rule she set. Length is measured and printed; it is not enforced. **What is mine to check is whether a fact is recorded — not how she chooses to teach** |
| **A PROMISE A CHECK MAKES IN A COMMENT IS NOT A PROMISE THE CHECK KEEPS** *(v3.46)* | v3.42 said its derived exemption would fail itself the day The Human Body was written. It did not, and only asserting it made it so |
| **A CHECK THAT CANNOT FIRE IS INDISTINGUISHABLE FROM ONE THAT WORKS** *(v3.46)* | An assertion above `const errors = []` would have crashed instead of reporting, and passed every run because its branch never ran |
| **A NEW COURSE JOINS THE APP-WIDE DOOR WITH ITS FIRST MODULE, NOT ITS LAST** *(v3.46)* | The v3.25 failure refused in advance — and the `hb-` id collision it exposed proves the door was watching |
| **A FIELD RECORDED AND NEVER ASSERTED IS A FIELD THAT DRIFTS** *(v3.45)* | `video.minutes` sat on 192 lessons for twenty-one versions with no check reading it. Social Studies drifted to twelve over the ceiling; **85 Herbalism videos never got the field at all** |
| **A LESSON THAT IS WRITTEN IS A LESSON SHE CAN REACH** *(v3.25)* | **Widened past lessons at v3.39** — a rubric rendered nowhere is the same failure. **And widened again at v3.41: REACHING A LESSON IS NOT THE SAME AS REACHING IT IN AN ORDER SHE CAN READ.** The video was reachable and sat under every word of prose |
| **THE VIDEO RENDERS ABOVE THE PROSE** *(v3.41)* | Her Reading 3.46 is a **listening** score and she pressed read-aloud on 36 of 61 Check-In questions. **The thing that explains the lesson may not sit behind the reading it is there to make cheaper.** Held by `check-delivery` #7 |
| **A DOCUMENT DECLARED MISSING WHILE IT SITS ON THE SHELF IS THE SAME FAULT AS ONE REFERENCED AND ABSENT** *(v3.41)* | The v3.38 correction said `petal-pestle-v3.31-social-studies-shape.md` was never written. **It is 137 lines and always existed.** One fault sends a reader looking for nothing; the other throws away work that is there. **Both are fixed by opening the folder instead of trusting the note** |
| **AN ASSERTION THAT READS THE WRONG FUNCTION IS NOT A TEST OF ANYTHING** *(v3.41)* | v3.39's rule turned around. My own new assertion located the hook question in `lessonChunks()` — above every JSX element — so one of its branches could never fire. **Positions are measured only inside the component that renders** |
| **AN EXEMPTION THAT CANNOT EXPIRE IS NOT AN EXEMPTION, IT IS A HOLE** *(v3.42)* | v3.24 said an exemption fails once it is no longer needed, and v3.25 said a reading exemption is **derived, never a list**. The unlinked-subject exemption was a **list**, so nothing about it changed when the world under it did. It outlived its reason by twelve versions. **Derived from written lessons now, so it expires on its own** |
| **A LABEL AND A LINK MUST READ THE SAME FACT** *(v3.42)* | The 2:45 block's name knew about the rotation and its link did not, so the screen said "The Human Body" and would have opened Social Studies. **This is how a screen tells her one thing and does another** |
| **A LINK THAT WORKS BY ACCIDENT IS NOT A WORKING LINK** *(v3.42)* | Herbalism's link resolved correctly only because `LessonsView` happens to default to `APP_COURSES[0]`, which happens to be Herbalism. The two links added beside it would have opened the wrong course. **Every link names its course now** |
| **A CHECK THAT ASKS "IS THERE AN ANSWER" AND NEVER "IS THE ANSWER RIGHT" IS NO CHECK** *(v3.21, fired again at v3.42)* | Inverting the run-out rule handed the block to the garden with 48 lessons unread and **passed every assertion**, because the garden is a well-formed target |
| **A file named after one course never stands in for the whole app** *(v3.25)* | |
| **A reading exemption is DERIVED from what is taught, never a list** *(v3.25)* | **Per course since v3.34**, and **applied to the mini-lessons at v3.40** — the title is where a lesson declares the word it teaches |
| **Catch-up is per course** *(v3.25)* | |
| **A quarter exam waits for its quarter to be BUILT** *(v3.25)* | |
| **A gap printed too large is not honesty** *(v3.27)* | |
| **A raw id never reaches her screen** *(v3.28)* | |
| **A negative test that cannot fail is not a passing negative test** *(v3.28–v3.29)* | |
| **A lesson must agree with its module about its quarter** *(v3.29)* | |
| **The reading floor is measured per course** *(v3.29)* | |
| **Reword the content, do not loosen the check** *(v3.10, v3.30, v3.40)* | Nine of v3.40's own new lessons failed the new rules. Every one was reworded; **no cap moved.** |
| **THE TIMETABLE HAS NO CALENDAR** *(v3.31)* | **And the writing programme did not grow one either** — its order comes from days she has written |
| **Unwritten is not finished** *(v3.31)* | |
| **A course that has written every lesson it owes may not say `'building'`** *(v3.31)* | |
| **An optional argument that must always be passed is a rule nobody enforces** *(v3.31)* | **Fired for real at v3.40:** a one-argument `miniLessonFor` would have handed her lesson 1 every day |
| **Fewer lessons than elements is a survey, not a course** *(v3.31)* | |
| **A COUNT IS SETTLED FROM THE PRIMARY SOURCE** *(v3.32)* | Both rejected counts are in `georgiaSS4.js` with their source |
| **An element nothing can own is an element nothing teaches** *(v3.32)* | |
| **A DROPPED ELEMENT IS DECLARED BY A PERSON** *(v3.33)* | **A declaration that goes stale fails the build.** |
| **A RULE ENFORCED ON THE CONFIG AND NOT ON THE PAPER THE CHILD IS HANDED IS HALF A RULE** *(v3.34)* | |
| **A number that happens to be correct today is not a measurement** *(v3.34)* | |
| **The simulation moves; the standard does not** *(v3.35)* | |
| **ADVOCACY ORGANISATIONS ARE NOT EDUCATIONAL PUBLISHERS** *(v3.37)* | |
| **THE JOURNAL IS NEVER GRADED AND NEVER CORRECTED** *(v3.38)* | |
| **A check that locates the thing it guards by a comment is guarding the comment** *(v3.38)* | |
| **SHE READS THE RUBRIC BEFORE SHE WRITES A WORD** *(v3.39)* | |
| **READ-ALOUD IS RECORDED, NEVER HIDDEN** *(v3.39)* | |
| **A mutation that lands in the wrong function is not a test of anything** *(v3.39)* | |
| **A LESSON MAY ONLY ASK FOR WHAT IT GAVE HER** *(v3.40)* | Gigi's find. Every closed answer must come from its own lesson — **and fewer than forty closed tasks fails the build, so the rule cannot be switched off by going all-open** |
| **EVERY LESSON IS WRITTEN AT HER LEARNING LEVEL, NOT HER AGE** *(v3.40)* | Her results doc said it on Aug 13 and nothing enforced it on this content. **Grammar & Usage 2.15 is the number the caps start from** |
| **A programme with no sequence is not a programme** *(v3.40)* | The order IS the teaching. Sentence work first, uninterrupted, because Gigi chose it — so it is a check rather than a habit |

---

## 5. Measured against Mission Control

| # | Gap in hers | Status |
|---|---|---|
| 1 | No practice mastery gate | **CLOSED — v3.1** |
| 2 | No exit ticket | **CLOSED — v3.1** |
| 3 | This log's numbers are hand-written and can drift | **STILL OPEN, and it has cost something real three times.** A videos doc stood for a fortnight holding a rejected list. `curriculumPlan.js` claimed standards that did not exist. And both this log and the master plan referenced a v3.31 document that was never written. His `STATUS.md` is generated from the code. |

**The rule behind #3:** decisions and reasons are written by hand; anything countable is generated.

---

## 6. Open items

| Item | Status |
|---|---|
| **The Human Body, 64 lessons** | ⭐ **DONE — v3.53. 64 of 64, and with it EVERY LESSON THIS APP OWES: 256 of 256.** Enrichment, no Georgia element, written last on purpose. Full year, confirmed by Gigi at v3.31. §31 of the master plan. |
| **Publishing to a web address — Azianna's Chromebook** | ⭐ **DONE, Aug 23 2026 — and since Aug 24 it deploys from GitHub, not by dragging.** `github.com/philissa-sketch/petal-pestle-academy`, public, with `netlify.toml` setting the build command to `npm run check && npm run build` so **a red check fails the deploy and the old site stays up.** Her export, her backup and `azianna-diagnostic-results.md` are excluded at any visibility — Mission Control's own rule, mirrored. **Original entry:** **DONE, Aug 23 2026 — IT IS LIVE AND SHE HAS HER WORK.** Built, published to Netlify, opened on the Acer Chromebook, backup imported, **everything restored and working.** No Node, no Linux, no terminal on her machine. **The old laptop's copy is now frozen history — the Chromebook holds the live one.** Linux is not supported on her Acer, so the app is built and uploaded instead. **All 256 lessons already embed a YouTube video, so the app has never worked offline** — the local-first promise is about HER DATA, not the network. Netlify password protection is Pro-only, so **the address is public: nothing personal may ever go in `public/`,** asserted by `check-publish-safety`. **Keep the site name unchanged — a new address is a new, empty app.** |
| **Three held skill terms — `balance`, `volume`, `shape`** | **Needs Gigi's call, and it is the `weight` check run three more times:** list the carrying lessons, read the titles. Held out of the catalog rather than guessed in, because this is the group Phase 4 aims score-targeted games with. **`check-skills` fails if one is added without being taken off the held list.** Also open: whether `model` comes back — the one retirement most likely to be reversed. |
| **A Check-In re-take** | ⭐ **SHE IS SITTING IT NOW — Aug 24 2026, Gigi.** The item below is kept because it is the reasoning that produced the sitting, and because §3.10.8's delta needs the Aug 13 baseline it names. **WHAT HAPPENS NEXT, so it is not missed:** take a fresh export off the Chromebook when she finishes, re-read her levels off the data rather than off any document, update `azianna-diagnostic-results.md` from it a second time — **its Aug 13 baseline in §1b is what the delta is measured against and must not be overwritten** — and then **review the Quarter 4 reading cap**, which §34 held at 14 explicitly pending this. **The four strands still `settled: false` are the ones to watch.** *(Original entry:)* **It gated the Q4 reading cap.** §3.10.8: *"growth is a delta on ONE INSTRUMENT'S SCALE"* — this app has one, the Check-In, and `diagnosticCompletedAt` is written **once**. The weekly and quarterly tests cannot stand in: they carry `evidenceSource: 'test'`, draw from course banks with **no strand**, and §3.10.6 keeps that wall up on purpose. **Suggested cadence: baseline, mid-year, year-end — not quarterly.** 61 questions and ~71 minutes across four sittings is a lot of asking a nine-year-old things nobody taught her. Targeted single-strand re-measures in between, as already done for Geometry and Measurement. |
| **Herbalism M13–M15 prose — 18 lessons to split** | **Recorded debt, v3.70, not urgent.** 16–19.6 words a sentence against a cap of 14; eighteen for eighteen. The oldest content in the app, written before the ramp policy existed. **A sentence-splitting job, not a rewrite of what is taught.** Q4 is April. Plus 19 milder ones — Herbalism M10–M11, Science Lab Q1 M3–M4, `body-m16-01`, and three real long-word breaches. **All 37 are in `KNOWN_OVER` and the list is a ratchet: it cannot grow quietly and a fixed lesson fails until its line is deleted.** |
| **The 13 flat cards have no `course`, `quarter`, `week` or `standards`** | **Bigger than "still flat cards".** `hb-1-01`–`hb-1-13` cannot be filtered by course, capped by quarter, or reported to Georgia. **They DO carry `words:`**, so the Phase 2 skill tagging would tag thirteen lessons that do not know what course they are in. **Fix before the tagging pass, not after.** Listed as `UNCAPPABLE` in `readingCaps.js`; `check-lesson-prose` fails if the list grows. |
| **`check-sources` cannot see a regex literal** | **Known, not fixed — same family as the curly-apostrophe rule for JSX.** `blankNonCode()` strips comments and strings before counting brackets and has no concept of a regex, so a quote inside a character class opens a string in its parser and it reports a phantom unbalanced bracket. **It only bites in `src/`, which is the only tree it scans.** Worked around in `readingLoad.js` with `\u0027`/`\u0022` escapes. |
| **Gigi to read the 72 rebuilt mini-lessons** | `writing-mini-lessons-v3.40.html`, delivered and saved as an artifact. **She should judge the level herself before Azianna meets it.** |
| **`Lh8wC-qoqgw` — the 3/5 Compromise video, Social Studies lesson 10** | ⭐ **APPROVED — Gigi, Aug 24 2026: *"she can watch the video."*** 5:13 and squarely about slavery. **Recorded honestly: Gigi waived her own preview rather than having watched it** — the open item asked her to watch it first and she cleared it without. That is her call to make and it is written down as what it is, so nobody later reads this as *"previewed and approved"*. |
| **The advocacy exclusion** | **Needs Gigi's call.** It cost two lessons their own video; alternates are in the files. |
| **A Black-scientists module for The Science Lab** | **Needs Gigi's call.** Two real finds that fit no lesson there. |
| **The Ubongo / Super Sema question** | **Needs Gigi's call** — Science Lab lesson 23 |
| **The Khan grades screen** | ⭐ **DONE — v3.74 to v3.76.** Fraction entry (*"I'll type 8/10"*) → percent → letter on **the plus/minus ladder read off Lamar's real report card**, not off his docs, which were behind his running app. A **row per unit** with an inline picker, no dropdowns, matching his mission card. **The Course Challenge has its own row**, never marking a unit done and never averaged with the units. **And the wiring bug underneath it all:** `addKhanGrade` wrote neither `courseId` nor `unitN`, so recording a grade **never advanced her** — diagnosed on Aug 23 as *"not a bug"* and it was one. `check-khan-advance` (#31). |
| **§4.1 remainder** | Gradebook tabs per course by quarter · My Levels holding her grades · a browsable projects area |
| **§4.4 weekly spelling and vocabulary** | 360 + 360 words. **None of Lamar's 720 transfer.** |
| **⚠️ Three questions a morning, against a 2,495-question bank** | **GIGI'S CALL, and the only open question about how her day runs.** Re-measured on the finished app at v3.53: the average question is now retrieved **1.94 times** across 260 simulated days, down from 2.0 at v3.52 — **below the old threshold, exactly as v3.52 predicted**. Mean gap **64.6 days**, longest **168**. The warm-up introduces 5 questions all year; **924 of the 929 she meets arrive through a weekly test**. Nothing is broken and nothing nags her — that was the design. Whether three is still right is a decision about her morning. |
| **Azianna's laptop** | ⭐ **SUPERSEDED, Aug 23 2026.** She is on the **Acer Chromebook** now, running the published app at v3.73 — the newest version, not v2.8. **The old laptop is no longer the machine that matters**; keep it untouched until the Chromebook has been exported from at least once. |
| **Lessons still to write** | **NONE. 256 of 256.** This row stays because it was the app's oldest number and it is worth seeing it reach the end. |
| **The 13 re-homed Herbalism lessons are still flat cards** | The one quality gap inside a course marked complete |
| **Lesson-prose readability for the COURSES** | ⭐ **DONE — v3.70, and this row was stale for four versions.** All 256 measured, 243 against a real cap plus 13 flat cards that cannot be capped and are listed rather than skipped. **37 over their own cap**, held in `KNOWN_OVER` as a **ratchet** — it cannot grow quietly and a fixed lesson fails until its line is deleted. `check-lesson-prose` (#27). **A status row that still described the job as not started, in the file whose job is to say what is done.** |
| **Generated `STATUS.md` / the Annual Progress Report** | **Would have prevented three count-drift bugs, a videos doc wrong for a fortnight, a standards claim that was never true, a referenced document that did not exist — and the FOUR more found on Aug 18: `192 of 192` videos in three places, `179` duplicate-content lessons, the laptop needing `v3.40` in one document and `v3.53` in the other, and `1,855 questions across three courses` in the checks table. All four corrected at v3.55. Lamar's app generates his from code; this one still cannot.** |
| **⭐ THE BLUEPRINT CONFORMANCE AUDIT** | **`claude/blueprint-conformance-audit.md`, Aug 18.** The app is now measured against `BLUEPRINT_A_LOCAL_FIRST.md` v1.1. **The work queue it produced is §32 of the master plan**, ordered by the blueprint's own §7.6 batching. Highest value-per-hour item: **the export guard reads the export, not the schema** — ~30 minutes, and it closes the bug class it was written for from the side it currently misses. |
| **✅ A fresh export off Azianna's laptop** | ⭐ **DONE, Aug 23 2026 — asked for four times and finally taken.** Exported from the old laptop and imported into the Chromebook; her levels, journal, Petals and Georgia hours all came across. **The export habit now moves to the Chromebook, which holds the only live copy.** |
| **`check-jsx`'s apostrophe false positive** | Known, recorded, not fixed. **Use a curly apostrophe in JSX text.** |
